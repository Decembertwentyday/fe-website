# EtchMarket 项目架构 · 代码全面解读手册

> **目标读者**：前端初学者 / Web3 入门者  
> **阅读方式**：从头到尾按顺序阅读，遇到不懂的名词先跳过，看完全文再回来  
> **最终目标**：读完本手册后，你能看懂每一行代码，理解项目架构，并具备独立修改和扩展的能力

---

## 目录

1. [项目是什么？做什么的？](#一项目是什么做什么的)
2. [技术栈全解析（每个库是干什么的）](#二技术栈全解析)
3. [项目整体架构设计思路](#三项目整体架构设计思路)
4. [目录结构逐层解读](#四目录结构逐层解读)
5. [代码入口：从哪里开始读？](#五代码入口从哪里开始读)
6. [核心流程逐步拆解](#六核心流程逐步拆解)
7. [关键概念名词解释（Web3 小白必读）](#七关键概念名词解释)
8. [核心文件逐行注释解读（第一批）](#八核心文件逐行注释解读)
9. [如果你是新手，怎么从零理解这个项目？](#九如果你是新手怎么从零理解这个项目)
10. [第二批内容预告](#十第二批内容预告)

---

## 一、项目是什么？做什么的？

### 1.1 项目名称与定位

项目名称：**EtchMarket**  
口号：_The First Community-driven Ethscriptions Indexer、Marketplace、Dex_  
中文意思：**第一个社区驱动的以太坊铭文 索引器 + 交易市场 + 去中心化交易所**

### 1.2 通俗解释

想象一下，你在以太坊区块链上刻了一段文字、一张图片、或者一段 JSON 数据——这就叫 **Ethscription（以太坊铭文）**。就像在石头上刻字，永久保存在链上，无法篡改。

EtchMarket 是这些铭文的"淘宝 + 区块链浏览器"：

- **浏览器功能**：你可以查看所有人刻了什么铭文（Explorer）
- **市场功能**：你可以挂单出售或购买别人的铭文（Marketplace）
- **代币功能**：支持铭文协议发行的代币（ERC-20 风格的铭文代币）
- **交换功能**：不同铭文代币之间可以互换（DEX/Swap）
- **Launchpad 功能**：新铭文项目可以在这里首发（类似 IDO）
- **跨链桥功能**：铭文资产可以跨链（Bridge）

### 1.3 核心业务模块

```
EtchMarket 核心功能
├── 首页（Explorer） ← 最新铭文动态，搜索入口
├── 市场（Market）   ← NFT集合交易市场
│   ├── NFT 类别
│   ├── 代币(Token) 类别
│   └── 域名(Domain) 类别
├── 资产（Asset）    ← 个人钱包资产管理
├── 交换（Swap）     ← 代币兑换 DEX
├── 质押（Staking）  ← 代币质押挖矿
├── 发射台（Launchpad） ← 新项目首发
├── 跨链桥（Bridge） ← 资产跨链
├── 铭文（Ethscriptions） ← 铭文详情
└── 搜索（Search）   ← 全局搜索
```

---

## 二、技术栈全解析

> 不了解下面这些库，看代码会很吃力。这一节先让你知道"每个工具是做什么的"。

### 2.1 基础框架

| 库名         | 版本   | 作用     | 通俗解释                                 |
| ------------ | ------ | -------- | ---------------------------------------- |
| `next`       | 14.0.4 | 全栈框架 | 就像汽车的发动机，整个项目跑在它上面     |
| `react`      | 18.2.0 | UI 库    | 用来写界面组件的库，比原生 HTML 方便很多 |
| `typescript` | 5.3.3  | 类型系统 | 给 JavaScript 加上类型检查，减少 bug     |

**为什么用 Next.js 而不是普通 React？**  
Next.js 提供了：服务端渲染（SEO 更好）、文件路由（按文件夹自动生成路由）、API 代理（解决跨域问题）、图片优化等功能。对于一个要被搜索引擎收录的 Web3 平台，这些都很重要。

### 2.2 UI 组件库

| 库名                                 | 作用      | 通俗解释                                       |
| ------------------------------------ | --------- | ---------------------------------------------- |
| `@mui/material`                      | UI 组件库 | 提供按钮、表格、弹窗等现成组件，不用从零写样式 |
| `@mui/icons-material`                | 图标库    | 提供各种 Material Design 图标                  |
| `@mui/x-data-grid`                   | 数据表格  | 专门用来展示大量数据的表格组件                 |
| `@emotion/react` + `@emotion/styled` | CSS-in-JS | 用 JavaScript 写 CSS 样式，支持动态样式        |

**为什么用 MUI？**  
MUI 是目前最成熟的 React UI 框架，有完整的设计规范，省去大量 UI 开发时间，且对响应式布局支持很好。

### 2.3 Web3 / 区块链相关库

| 库名                     | 作用              | 通俗解释                                                 |
| ------------------------ | ----------------- | -------------------------------------------------------- |
| `wagmi`                  | Web3 React Hooks  | 提供 `useAccount()`、`useConnect()` 等钩子，轻松操作钱包 |
| `@rainbow-me/rainbowkit` | 钱包连接 UI       | 提供漂亮的"连接钱包"弹窗，支持多种钱包                   |
| `ethers` (v5)            | 以太坊工具库      | 与区块链交互：发交易、读合约、签名等                     |
| `viem`                   | 以太坊工具库 (新) | wagmi 底层依赖，比 ethers 更现代的 API                   |
| `siwe`                   | 以太坊登录        | Sign-In with Ethereum，用钱包签名替代账号密码登录        |

**Web3 登录原理（重要！）**  
传统网站：输入用户名密码 → 服务器验证 → 发 token  
Web3 网站：连接钱包 → 服务器给一段消息（nonce）→ 用私钥签名 → 服务器验证签名 → 发 token  
这样做的好处：不需要密码，只要你有私钥，就能证明你是钱包地址的主人。

### 2.4 状态管理

| 库名                  | 作用           | 通俗解释                                              |
| --------------------- | -------------- | ----------------------------------------------------- |
| `valtio`              | 全局状态管理   | 用 `proxy()` 创建响应式对象，比 Redux 简单 100 倍     |
| `react-query`         | 服务端数据缓存 | 自动管理 API 请求的缓存、加载状态、错误状态           |
| `immer` / `use-immer` | 不可变数据     | 写起来像普通对象，实际是不可变的，避免 React 渲染问题 |

**为什么同时用 valtio 和 react-query？**

- `valtio`：管理"用户登录状态"、"购物车"等**全局持久状态**
- `react-query`：管理"API 请求返回的列表数据"等**服务端数据**  
  两者职责不同，配合使用。

### 2.5 数据请求

| 库名    | 作用      | 通俗解释                                |
| ------- | --------- | --------------------------------------- |
| `axios` | HTTP 请求 | 比原生 fetch 更强大，支持拦截器、超时等 |

### 2.6 其他工具库

| 库名              | 作用     | 通俗解释                                           |
| ----------------- | -------- | -------------------------------------------------- |
| `dayjs`           | 日期处理 | 比原生 Date 好用的日期库                           |
| `bignumber.js`    | 大数运算 | 区块链金额精度很高（18位小数），普通数字计算会出错 |
| `lodash-es`       | 工具函数 | 各种常用函数（深拷贝、数组操作等）                 |
| `next-intl`       | 国际化   | 多语言支持（中文/英文切换）                        |
| `@sentry/nextjs`  | 错误监控 | 线上报错自动收集，方便排查问题                     |
| `react-hot-toast` | 消息提示 | 弹出"操作成功"/"操作失败"提示框                    |
| `react-hook-form` | 表单处理 | 管理表单输入、验证、提交，比受控组件简单           |

---

## 三、项目整体架构设计思路

### 3.1 架构图

```
┌─────────────────────────────────────────────────────────────┐
│                        用户浏览器                             │
└────────────────────────────┬────────────────────────────────┘
                             │ HTTP 请求
┌────────────────────────────▼────────────────────────────────┐
│                    Next.js 应用层                             │
│  ┌──────────────────────────────────────────────────────┐   │
│  │                   App Router（路由）                   │   │
│  │  /              → src/app/home/                      │   │
│  │  /market        → src/app/market/                   │   │
│  │  /swap          → src/app/swap/                     │   │
│  │  /staking       → src/app/staking/                  │   │
│  │  /asset         → src/app/asset/                    │   │
│  │  /launchpad     → src/app/launchpad/                │   │
│  │  /bridge        → src/app/bridge/                   │   │
│  │  /search        → src/app/search/                   │   │
│  │  /transactions  → src/app/transactions/             │   │
│  │  /ethscriptions → src/app/ethscriptions/            │   │
│  │  /tokens        → src/app/tokens/                   │   │
│  │  /erc20s        → src/app/erc20s/                   │   │
│  │  /owner         → src/app/owner/                    │   │
│  └──────────────────────────────────────────────────────┘   │
│                                                              │
│  ┌─────────────┐  ┌──────────────┐  ┌───────────────────┐  │
│  │  全局状态     │  │   UI 组件层   │  │   服务层(API)      │  │
│  │  (valtio)   │  │  components/ │  │  services/        │  │
│  │  GlobalStore│  │  containers/ │  │  ├── ethscriptions│  │
│  │  CartStore  │  └──────────────┘  │  ├── marketplace  │  │
│  │  BulkStore  │                    │  ├── launchpad    │  │
│  └─────────────┘                    │  ├── vault        │  │
│                                     │  ├── did          │  │
│  ┌─────────────┐                    │  ├── domain       │  │
│  │  自定义 Hooks │                    │  └── facet        │  │
│  │  hooks/     │                    └───────────────────┘  │
│  └─────────────┘                                            │
└──────────────────────────────┬──────────────────────────────┘
                               │ API 请求（被 Next.js 代理）
        ┌──────────────────────▼──────────────────────┐
        │           后端 API 服务                        │
        │  https://www.etch.market/api/...             │
        └─────────────────────────────────────────────┘
                               │ 读取数据
        ┌──────────────────────▼──────────────────────┐
        │           以太坊区块链                         │
        │  (索引链上的 Ethscriptions 数据)               │
        └─────────────────────────────────────────────┘
```

### 3.2 数据流向（从用户操作到显示）

```
用户点击"购买"按钮
     ↓
React 组件事件处理函数 (onClick)
     ↓
调用 services/marketplace/index.ts 里的 API 方法
     ↓
etchClient（axios 实例）发送 HTTP 请求
     ↓
Next.js 代理层（next.config.js 的 rewrites 配置）
     ↓
真实后端服务（etch.market API）
     ↓
返回 JSON 数据
     ↓
react-query 缓存数据
     ↓
React 组件重新渲染，显示结果
     ↓
如果需要链上操作（签名、交易）
     ↓
wagmi hooks（useContractWrite 等）
     ↓
RainbowKit 弹出确认框
     ↓
用户在 MetaMask/OKX 钱包确认
     ↓
交易广播到以太坊网络
```

### 3.3 分层设计原则

这个项目采用了**三层架构**：

**第一层：页面层（app/）**

- 负责路由和页面布局
- 尽量轻，不写太多业务逻辑
- 组合容器组件来搭建页面

**第二层：容器层（containers/）**

- 包含业务逻辑的"智能组件"
- 知道如何请求数据、处理状态
- 把处理好的数据传给展示组件

**第三层：组件层（components/）**

- 纯展示型"哑组件"（Dumb Components）
- 只接受 props，展示 UI
- 不关心数据从哪里来

**为什么要这样分层？**

- **可维护性**：改 UI 不用动业务逻辑，改接口不用动 UI
- **可复用性**：纯展示组件可以在不同页面复用
- **可测试性**：纯展示组件很容易写单元测试

---

## 四、目录结构逐层解读

```
fe-website_1/
│
├── 📄 package.json          ← 项目依赖和脚本配置（相当于项目的"身份证"）
├── 📄 next.config.js        ← Next.js 框架配置（路由代理、webpack配置等）
├── 📄 tsconfig.json         ← TypeScript 编译配置（@ 路径别名在这里定义）
├── 📄 vercel.json           ← Vercel 部署配置
├── 📄 Dockerfile            ← Docker 容器化配置（用于服务器部署）
├── 📄 sentry.*.config.ts    ← Sentry 错误监控配置（client/server/edge 三端）
│
├── 📁 public/               ← 静态资源（图片等，直接通过 URL 访问）
│   └── images/
│
└── 📁 src/                  ← 所有源代码（Source Code）
    │
    ├── 📄 middleware1.ts    ← Next.js 中间件（请求拦截、权限验证）
    │
    ├── 📁 app/              ← ★ Next.js App Router 核心目录
    │   │
    │   ├── 📄 layout.tsx            ← 根布局（所有页面共享的最外层结构）
    │   ├── 📄 page.tsx              ← 首页（就一行：导入 Home 组件）
    │   ├── 📄 providers.tsx         ← 全局 Provider 配置（钱包、主题、Toast等）
    │   ├── 📄 providersMiddleware.tsx ← Provider 中间件（登录后获取用户信息）
    │   ├── 📄 authenticationAdapter.ts ← SIWE 认证适配器（Web3 登录逻辑）
    │   │
    │   ├── 📁 home/         ← 首页模块
    │   ├── 📁 market/       ← 市场模块（NFT/Token/Domain 交易）
    │   ├── 📁 asset/        ← 资产模块（我的资产）
    │   ├── 📁 swap/         ← 兑换模块（DEX）
    │   ├── 📁 staking/      ← 质押模块
    │   ├── 📁 launchpad/    ← 发射台模块
    │   ├── 📁 bridge/       ← 跨链桥模块
    │   ├── 📁 search/       ← 搜索结果页
    │   ├── 📁 transactions/ ← 交易记录页
    │   ├── 📁 ethscriptions/ ← 铭文详情页
    │   ├── 📁 tokens/       ← 代币详情页
    │   ├── 📁 erc20s/       ← ERC20 铭文代币页
    │   ├── 📁 owner/        ← 用户主页（钱包地址对应的个人页）
    │   └── 📁 bulk/         ← 批量操作模块
    │
    ├── 📁 assets/           ← 静态资源（代码引用的资源）
    │   ├── global.css       ← 全局 CSS 样式
    │   ├── icons/           ← SVG 图标文件
    │   ├── images/          ← 图片文件
    │   └── styles/          ← 其他样式文件
    │
    ├── 📁 components/       ← ★ 通用 UI 组件（纯展示，无业务逻辑）
    │   ├── Pagination.tsx   ← 分页组件
    │   ├── SearchInput.tsx  ← 搜索框组件
    │   ├── NftImage.tsx     ← NFT 图片展示组件
    │   ├── LineChart.tsx    ← 折线图组件
    │   ├── WalletConnectButton.tsx ← 连接钱包按钮
    │   └── ...
    │
    ├── 📁 containers/       ← ★ 业务容器组件（含数据请求和业务逻辑）
    │   ├── Header.tsx       ← 顶部导航栏
    │   ├── Footer.tsx       ← 底部
    │   ├── CollectionList/  ← 集合列表（市场首页）
    │   ├── CartDrawer/      ← 购物车抽屉
    │   ├── EthscriptionBox/ ← 铭文展示框
    │   └── ...
    │
    ├── 📁 constants/        ← 常量配置
    │   ├── config.ts        ← 环境配置（主网/测试网 API 地址、链配置）
    │   ├── abis.ts          ← 智能合约 ABI（合约接口定义）
    │   ├── index.ts         ← 其他常量
    │   └── theme.ts         ← MUI 主题配置（颜色、字体等）
    │
    ├── 📁 hooks/            ← 自定义 React Hooks
    │   └── ...              ← 封装复用逻辑（如：useNftList, useCartLogic 等）
    │
    ├── 📁 services/         ← ★ API 服务层（所有后端接口调用）
    │   ├── index.ts         ← 服务入口，导出统一的 services 对象
    │   ├── types.ts         ← 通用类型定义（接口响应格式等）
    │   ├── getQueryParams.ts ← URL 查询参数工具函数
    │   ├── network/         ← HTTP 客户端（axios 封装）
    │   │   ├── ApiClient.ts ← 基础 API 客户端类
    │   │   ├── etchClient.ts ← EtchMarket 后端客户端（含认证拦截器）
    │   │   └── facetClient.ts ← Facet 链客户端
    │   ├── ethscriptions/   ← 铭文相关接口
    │   ├── marketpalce/     ← 市场相关接口（注意：marketplace 拼写有误，是 typo）
    │   ├── launchpad/       ← 发射台接口
    │   ├── did/             ← 身份认证接口（DID = 去中心化身份）
    │   ├── domain/          ← 域名相关接口
    │   ├── vault/           ← 金库接口
    │   ├── facet/           ← Facet 链接口
    │   └── evm/             ← EVM 链直接读取（不经过后端）
    │
    ├── 📁 stores/           ← 全局状态（valtio）
    │   ├── GlobalStore.ts   ← 全局状态（登录状态、OG资格等）
    │   ├── CartStore.ts     ← 购物车状态
    │   ├── BulkListingStore.ts ← 批量挂单状态
    │   ├── EthscriptionsStore.ts ← 铭文数据缓存
    │   └── FacetSwapStore.ts ← Swap 状态
    │
    ├── 📁 messages/         ← 国际化文案（中/英文翻译）
    │
    ├── 📁 types/            ← TypeScript 类型定义
    │
    ├── 📁 utils/            ← 工具函数
    │
    ├── 📁 wallets/          ← 自定义钱包配置（如 GateWallet）
    │
    ├── 📁 typechain-types/  ← 自动生成的合约类型（由 typechain 生成）
    └── 📁 typechain-types-erc20/ ← ERC20 合约类型
```

---

## 五、代码入口：从哪里开始读？

### 5.1 推荐阅读顺序（新手路线图）

```
第1步：了解项目配置
  package.json → next.config.js → src/constants/config.ts

第2步：了解应用骨架
  src/app/layout.tsx → src/app/providers.tsx → src/app/providersMiddleware.tsx

第3步：了解首页
  src/app/page.tsx → src/app/home/index.tsx → src/app/home/HomeLayout.tsx

第4步：了解导航
  src/containers/Header.tsx → src/containers/NavigationAPP.tsx

第5步：了解全局状态
  src/stores/GlobalStore.ts → src/app/authenticationAdapter.ts

第6步：了解数据请求
  src/services/index.ts → src/services/network/ApiClient.ts
  → src/services/network/etchClient.ts → src/services/ethscriptions/index.ts

第7步：了解一个完整业务模块（推荐从市场开始）
  src/app/market/page.tsx → src/containers/CollectionList/ → src/services/marketpalce/

第8步：深入了解 Web3 部分
  src/app/authenticationAdapter.ts → src/constants/abis.ts → src/services/evm/
```

### 5.2 为什么从 layout.tsx 开始？

Next.js 的 App Router 中，`layout.tsx` 是**最外层的包裹容器**。所有页面都被包裹在里面。就像一个网页的 HTML 骨架：

```
layout.tsx (整个应用的外壳)
  ├── <html>
  │   ├── <body>
  │   │   ├── <Providers>          ← 提供钱包连接、主题、Toast 等全局功能
  │   │   │   ├── <Header />       ← 顶部导航栏（每个页面都有）
  │   │   │   ├── <ProvidersMiddleware>
  │   │   │   │   └── {children}  ← 这里放具体页面的内容
  │   │   │   └── <Analytics />   ← Vercel 统计（隐藏的）
  │   │   └── </Providers>
  │   └── </body>
  └── </html>
```

---

## 六、核心流程逐步拆解

### 6.1 钱包连接流程

```
用户点击右上角"Connect Wallet"按钮
  ↓
触发 RainbowKit 的连接钱包弹窗
  ↓
用户选择钱包（MetaMask / OKX / Rainbow...）
  ↓
wagmi 的 useConnect hook 处理连接
  ↓
连接成功：useAccount().address 有值了
  ↓
Header.tsx 的 useEffect 检测到 address 变化
  ↓
调用 GlobalStore.initRainbowKitAuthStatus(address)
  ↓
从 localStorage 读取该地址的 token
  ↓
如果有 token → 设置为 'authenticated' 状态（已登录）
如果没有 token → 设置为 'unauthenticated' 状态（未登录）
```

### 6.2 Web3 登录（SIWE）流程

```
用户点击"Sign In"（钱包已连接但未登录）
  ↓
authenticationAdapter.getNonce() 被调用
  ↓
向后端请求一个随机数 (nonce)
  ↓
authenticationAdapter.createMessage() 创建 SIWE 消息
  ↓
消息格式："Sign in with Ethereum to the app. (nonce: xxx)"
  ↓
RainbowKit 弹出签名确认框（MetaMask 里的 Sign 操作）
  ↓
用户确认签名（注意：这不花 Gas，只是本地签名）
  ↓
authenticationAdapter.verify() 被调用
  ↓
把签名发给后端验证
  ↓
后端返回 accessToken（JWT）
  ↓
GlobalStore.setRainbowKitAuthStatus('authenticated', address, token)
  ↓
token 存入 localStorage
  ↓
后续所有 API 请求自动带上 Bearer token（在 etchClient 的拦截器里）
```

### 6.3 页面数据加载流程（以市场页为例）

```
用户访问 /market
  ↓
Next.js 渲染 src/app/market/page.tsx
  ↓
page.tsx 渲染 <CollectionList /> 组件
  ↓
CollectionList 组件内部调用 react-query 的 useQuery
  ↓
触发 services.marketplace.getCollections() API 请求
  ↓
etchClient（axios）发送请求到 /api/markets/collections
  ↓
next.config.js 的 rewrites 规则代理到 https://www.etch.market/api/markets/collections
  ↓
后端返回集合列表 JSON
  ↓
react-query 缓存数据，触发组件重新渲染
  ↓
CollectionList 渲染集合卡片列表
```

---

## 七、关键概念名词解释

> 这部分专门为 Web3 零基础的同学准备，如果你已经了解 Web3，可以跳过。

### 7.1 什么是 Ethscription（以太坊铭文）？

**类比理解**：比特币有"铭文"（Ordinals），可以把数据刻在比特币上。Ethscription 是以太坊版本的铭文——把数据放在以太坊交易的 calldata 字段里。

技术细节：当你向某个地址发送一笔交易，交易的输入数据（calldata）是 `data:...` 开头时，Ethscription 索引器会把它识别为一个铭文，并记录谁创建的、创建时间、内容是什么。

### 7.2 什么是 ERC-20 铭文代币？

传统 ERC-20 代币：通过部署智能合约来发行  
铭文 ERC-20：通过一系列约定格式的以太坊交易来模拟代币操作（mint/transfer 等）

这个项目里的"erc20s"模块就是处理这类铭文代币。

### 7.3 什么是 DID？

DID = Decentralized Identity（去中心化身份）  
在这个项目里，DID 主要负责：

- 用户认证（SIWE 登录）
- OG Pass 资格验证（早期用户特权）

### 7.4 什么是 OG Pass？

OG = Original Gangster（早期支持者）  
OG Pass 是早期用户的特殊资格，持有者可以享受某些优先权益（比如 Launchpad 优先购买权）。

项目里通过 `GlobalStore.isOgPass` 来记录当前用户是否持有 OG Pass。

### 7.5 什么是 Facet？

Facet 是一个以太坊 L2（第二层）网络，使用 calldata 实现的"软合约"。这个项目的 Swap（兑换）功能部分基于 Facet 链实现。

### 7.6 什么是 Vault（金库）？

Vault 是托管合约，用于存放 NFT 进行挂单销售。当你挂单出售一个铭文时，它会被存入合约，买家支付 ETH 后，合约自动把铭文转给买家，把 ETH 转给卖家。

---

## 八、核心文件逐行注释解读

### 8.1 `src/constants/config.ts` — 环境配置文件

```typescript
// ============================================================
// 文件说明：这是整个项目的"配置开关"文件
// 作用：通过切换不同的配置对象，可以让项目运行在主网或测试网
// 为什么这样做：开发时用测试网（不花真实 ETH），上线时用主网
// ============================================================

import { mainnet, goerli } from 'wagmi/chains';
// ↑ 从 wagmi 导入两个链的配置信息
// mainnet = 以太坊主网（真实交易，花真实 ETH）
// goerli = 以太坊测试网（免费的测试 ETH，不值钱）

// FACET 主网配置
// Facet 是以太坊的 L2 网络，用于处理 Swap 交换功能
const FACET_MAIN_CONFIG = {
  SCAN_URL: 'https://api.facet.org/', // Facet 主网 API 地址
  SCAN_FE_URL: 'https://facetscan.com', // Facet 区块链浏览器地址（给用户看交易）
  RECEIVE_ADDRESS: '0x00000000000000000000000000000000000FacE7', // Facet 接收合约地址（固定的）
  FETH_ADDRESS: '0x1673540243e793b0e77c038d4a88448eff524dce', // FETH 代币合约地址
};

// FACET 测试网配置（结构和主网一样，只是地址不同）
const FACET_GOERLI_CONFIG = {
  SCAN_URL: 'https://goerli-api.facet.org/',
  SCAN_FE_URL: 'https://goerli.facetscan.com',
  RECEIVE_ADDRESS: '0x00000000000000000000000000000000000FacE7',
  FETH_ADDRESS: '0xcffc7fbd459d4c028163029d0db0fa26f44b0ed1',
};

// 后端 API 主网配置
const SERVER_MAIN_CONFIG = {
  BASE_URL: '/api', // 请求前缀（被 next.config.js 代理到真实后端）
  CHAIN: [mainnet], // 使用以太坊主网（数组格式，wagmi 支持多链）
};

// 后端 API 测试网配置
const SERVER_GOERLI_CONFIG = {
  BASE_URL: '/api-goerli', // 不同的代理前缀，被代理到测试网后端
  CHAIN: [goerli],
};

// ★ 关键配置开关 ★
// 修改这两行来切换环境：
// 主网：SERVER_MAIN_CONFIG / FACET_MAIN_CONFIG
// 测试网：SERVER_GOERLI_CONFIG / FACET_GOERLI_CONFIG
export const SERVER_CONFIG = SERVER_MAIN_CONFIG; // SERVER_GOERLI_CONFIG;
export const FACET_CONFIG = FACET_MAIN_CONFIG; // FACET_GOERLI_CONFIG;
```

---

### 8.2 `next.config.js` — Next.js 框架配置

```javascript
// ============================================================
// 文件说明：Next.js 框架配置文件
// 作用：
//   1. 配置 API 代理（解决浏览器跨域问题）
//   2. 配置 SVG 图标支持
//   3. 集成 Sentry 错误监控
// 为什么需要代理：浏览器的安全策略不允许直接跨域请求，
//   通过 Next.js 服务端来中转请求，可以绕过跨域限制
// ============================================================

const { withSentryConfig } = require('@sentry/nextjs');
// ↑ 引入 Sentry 的 Next.js 配置包装函数

/** @type {import('next').NextConfig} */
// ↑ JSDoc 类型注释，让编辑器能提供代码提示

const nextConfig = {
  env: {},
  // ↑ 这里可以定义环境变量，供客户端代码访问（注意：不要放秘钥！）

  reactStrictMode: true,
  // ↑ React 严格模式：开发环境下组件会渲染两次，帮助发现潜在 bug
  // 为什么用：检测过时的 API 使用，副作用问题等

  output: 'standalone',
  // ↑ 构建输出模式：standalone 会把所有依赖打包在一起
  // 为什么用：Docker 部署时非常方便，不需要在容器里安装 node_modules

  compiler: {
    removeConsole: process.env.NODE_ENV === 'production',
    // ↑ 生产环境自动删除 console.log 语句
    // 为什么：1. 保护调试信息不泄露  2. 减小打包体积  3. 提升性能
  },

  experimental: {
    turbo: {
      rules: {
        '*.svg': ['@svgr/webpack'],
        // ↑ Turbopack（新版构建工具）中配置 SVG 的处理规则
        // 使用 @svgr/webpack 把 SVG 文件转为 React 组件
        // 这样可以直接 import LogoSVG from './logo.svg' 像组件一样使用
      },
    },
  },

  webpack(config, { isServer }) {
    // ↑ 自定义 webpack 配置（webpack 是 Next.js 默认的构建工具）

    config.module.rules.push({
      test: /\.svg$/i, // 匹配所有 .svg 文件
      use: ['@svgr/webpack'], // 用 @svgr/webpack 处理
      // ↑ 同上，在传统 webpack 模式下也支持 SVG 转 React 组件
    });

    config.resolve.fallback = { fs: false, net: false, tls: false };
    // ↑ 告诉 webpack：浏览器端不需要 Node.js 的 fs/net/tls 模块
    // 原因：某些第三方库（如 ethers.js）在 Node.js 环境里会用到这些模块，
    //       但在浏览器里运行时不需要，这里告诉 webpack 忽略它们

    return config;
  },

  async rewrites() {
    // ↑ URL 重写规则（代理配置）
    // 原理：用户访问 /api/xxx 时，Next.js 服务端会把请求转发到真实后端
    // 这样浏览器看到的是同域请求（不跨域），但实际请求转到了另一个域

    return [
      {
        source: '/api-goerli/:path*',
        // ↑ 匹配所有以 /api-goerli/ 开头的请求
        // :path* 表示后面任意路径

        destination: 'http://3.233.81.38:3002/:path*',
        // ↑ 转发到测试网后端服务器（IP 地址）
      },
      {
        source: '/api/:path*',
        destination: 'https://www.etch.market/api/:path*',
        // ↑ 转发到主网后端（etch.market 域名）
      },
      {
        source: '/api-release/:path*',
        destination: 'https://api.orbitrum.io/:path*',
        // ↑ 另一个后端服务（可能是发布环境或其他服务）
      },
    ];
  },
};

// 用 Sentry 包装 Next.js 配置
// 这会自动在构建时上传 source map 到 Sentry，方便线上错误定位到原始代码行
const sentryConfig = withSentryConfig(nextConfig, {
  silent: true, // 构建时不输出 Sentry 的日志
  org: 'etchmarket', // Sentry 组织名
  project: 'etchmarket-nextjs', // Sentry 项目名
  widenClientFileUpload: true, // 上传更完整的 source map（栈追踪更精准）
  transpileClientSDK: true, // 转译 Sentry SDK 以兼容旧浏览器
  tunnelRoute: '/monitoring', // 通过 Next.js 路由隧道发送 Sentry 数据（防广告拦截）
});

module.exports = sentryConfig;
```

---

### 8.3 `src/app/layout.tsx` — 根布局（所有页面的外壳）

```typescript
// ============================================================
// 文件说明：这是整个 Next.js 应用的根布局文件
// 类比：就像网页的 <html><head><body> 骨架结构
// 所有页面都会被包裹在这个组件里
//
// 重要概念：Next.js App Router 中，layout.tsx 会"持久存在"，
//   页面切换时 layout 不会重新渲染，只有 {children} 部分会变
// ============================================================

import { Analytics } from '@vercel/analytics/react';
// ↑ Vercel 的访问统计组件，会自动收集页面浏览量等数据
// 是 Vercel 平台的免费分析工具

import { Metadata } from 'next';
// ↑ 导入 Next.js 的 Metadata 类型，用于定义页面的 SEO 信息

import '@rainbow-me/rainbowkit/styles.css';
// ↑ RainbowKit 的 CSS 样式文件
// 必须在这里全局引入，否则钱包连接弹窗样式会丢失

import '@/assets/styles/index.css';
// ↑ 项目自定义全局样式
// @ 符号是路径别名，代表 src/ 目录（在 tsconfig.json 中配置）

import { Providers } from './providers';
// ↑ 导入全局 Provider 组件（见 providers.tsx 解析）

import Header from '@/containers/Header';
// ↑ 导入顶部导航栏组件

import '@/assets/global.css';
// ↑ 另一个全局 CSS 文件（两个 CSS 文件可能是历史遗留，各管一部分样式）

import { ProvidersMiddleware } from './providersMiddleware';
// ↑ 导入 Provider 中间件组件

// generateMetadata：Next.js 的特殊函数，用于生成页面的 SEO 元数据
// 这里定义了网站的标题、描述、关键词（显示在搜索引擎结果里）
export async function generateMetadata() {
  return {
    title: 'EtchMarket',
    description: 'EtchMarket, The First Community-driven Ethsciptions Indexer、Marketplace、Dex',
    keywords: 'ethereum ethscriptions nft marketplace web3 etch defi dex nft did',
    // ↑ 这些信息会被 Google 等搜索引擎抓取，影响 SEO 排名
  } as Metadata;
}

// generateStaticParams：定义支持的语言参数
// 这与 next-intl 国际化配合使用，但这里定义了中英两种语言
// 实际上这个函数在根 layout 中不起路由作用，更多是声明意图
export function generateStaticParams() {
  return [{ locale: 'en' }, { locale: 'zh' }];
}

// RootLayout：根布局组件
// children 参数：当前访问页面的具体内容（由 Next.js 自动传入）
export default async function RootLayout({ children }: { children: React.ReactNode }) {
  return (
    <html lang="en">
    {/* ↑ lang="en" 告诉浏览器和搜索引擎页面主要语言是英文 */}

      <body data-rk>
      {/* ↑ data-rk 是 RainbowKit 的自定义属性 */}
      {/* RainbowKit 通过这个属性来限定其 CSS 样式的作用范围 */}

        <Providers>
        {/* ↑ 把钱包连接、主题、Toast 等全局功能注入到整个应用 */}

          <Header />
          {/* ↑ 顶部导航栏，始终显示在所有页面顶部 */}

          <ProvidersMiddleware>
          {/* ↑ 中间件：用户登录后获取 OG Pass 等用户信息 */}

            {children}
            {/* ↑ 这里渲染具体页面的内容 */}
            {/* 访问 / 时渲染首页，访问 /market 时渲染市场页... */}

          </ProvidersMiddleware>
        </Providers>

        <Analytics />
        {/* ↑ Vercel 统计脚本，放在 Providers 外面是因为它不需要 Web3 上下文 */}

      </body>
    </html>
  );
}
```

---

### 8.4 `src/app/providers.tsx` — 全局 Provider 配置

```typescript
// ============================================================
// 文件说明：这是应用最核心的配置文件之一
// 作用：把所有"全局能力"注入到 React 组件树中
//
// 什么是 Provider？
// Provider 是 React Context 的实现模式：
// 父组件通过 Provider 向所有子孙组件"提供"某种能力或数据
// 子组件可以通过 hook（如 useAccount）直接访问，不需要层层传 props
//
// 这个文件里配置了：
//   1. react-query 的数据缓存能力
//   2. MUI 的主题系统（颜色、字体等）
//   3. Toast 消息提示
//   4. Wagmi 的 Web3 连接能力（连接以太坊）
//   5. RainbowKit 的钱包连接 UI
// ============================================================

'use client';
// ↑ Next.js App Router 中，这行声明这是"客户端组件"
// 原因：这个组件使用了 useState、useEffect 等只能在浏览器中运行的 Hook
// 没有这行声明的组件默认是"服务端组件"（在服务器上渲染）

import merge from 'lodash.merge';
// ↑ 深度合并对象的工具函数，用于合并 RainbowKit 主题

import { Toaster } from 'react-hot-toast';
// ↑ Toast 通知组件（显示"操作成功"/"操作失败"等提示）

import { useEffect, useState } from 'react';
// ↑ React 的两个核心 Hook：
// useState：组件内部的状态管理
// useEffect：副作用处理（组件挂载/更新时执行）

import customTheme from '@/constants/theme';
// ↑ 项目自定义的 MUI 主题（颜色配置等）

import { CssBaseline, ThemeProvider } from '@mui/material';
// ↑ ThemeProvider：把主题注入到所有 MUI 组件
// CssBaseline：重置浏览器默认样式（类似 CSS reset）

import { useSnapshot } from 'valtio';
// ↑ valtio 的 hook，用于订阅 proxy 状态的变化
// 当 store 里的数据变化时，使用 useSnapshot 的组件会自动重新渲染

import * as dayjs from 'dayjs';
import duration from 'dayjs/plugin/duration';
// ↑ dayjs 日期库和 duration 插件（用于计算时间差，如"3天5小时"）

// wagmi 相关导入
import { configureChains, createConfig, WagmiConfig } from 'wagmi';
// ↑ configureChains：配置支持的区块链网络
// createConfig：创建 wagmi 配置
// WagmiConfig：wagmi 的 Provider 组件

import { publicProvider } from 'wagmi/providers/public';
// ↑ 公共 RPC 节点提供者（免费，但有速率限制）
// RPC 节点是连接以太坊网络的"服务器入口"

// RainbowKit 相关导入
import {
  RainbowKitProvider,           // RainbowKit 的 Provider 组件
  RainbowKitAuthenticationProvider, // 支持登录认证的 Provider
  getDefaultWallets,            // 获取默认支持的钱包列表
  connectorsForWallets,         // 为指定钱包列表创建连接器
  Theme,                        // RainbowKit 主题类型
  darkTheme,                    // RainbowKit 深色主题
} from '@rainbow-me/rainbowkit';

// 导入各种钱包适配器
import {
  injectedWallet,       // 浏览器注入钱包（如 MetaMask 的 window.ethereum）
  rainbowWallet,        // Rainbow 钱包
  metaMaskWallet,       // MetaMask 钱包
  okxWallet,            // OKX 钱包
  walletConnectWallet,  // WalletConnect 协议（扫码连接移动端钱包）
  tokenPocketWallet,    // TokenPocket 钱包
} from '@rainbow-me/rainbowkit/wallets';

import { gateWallet } from '@/wallets/gateWallet';
// ↑ 自定义的 Gate.io 钱包适配器（RainbowKit 不内置，项目自己实现的）

import { QueryClient, QueryClientProvider } from 'react-query';
// ↑ react-query 的核心：
// QueryClient：缓存和状态管理实例
// QueryClientProvider：向整个应用注入 QueryClient

import { authenticationAdapter } from './authenticationAdapter';
// ↑ SIWE（Sign-In with Ethereum）认证适配器

import * as GlobalStore from '@/stores/GlobalStore';
// ↑ 全局状态 store（登录状态、OG Pass 等）

import { SERVER_CONFIG } from '@/constants/config';
// ↑ 环境配置（主网/测试网的链配置）

// 初始化 dayjs 的 duration 插件
dayjs.extend(duration);

// ★ 配置区块链网络和 RPC 节点
const { chains, publicClient, webSocketPublicClient } = configureChains(
  SERVER_CONFIG.CHAIN,   // 使用哪些链（主网或测试网）
  [publicProvider()]     // 使用公共 RPC 节点
);
// publicClient：普通 HTTP 请求客户端（读取区块链数据）
// webSocketPublicClient：WebSocket 客户端（订阅实时事件，如新区块）

// WalletConnect 项目 ID（在 WalletConnect Cloud 上申请的）
// 用于标识是哪个应用在使用 WalletConnect 协议
const projectId = 'c5e287b868fa350466d5a0d947a65042';

// 获取默认钱包列表（这会包含 MetaMask、Rainbow 等常见钱包）
const { wallets } = getDefaultWallets({
  appName: 'EtchMarket APP',  // 在钱包里显示的应用名称
  projectId,
  chains,
});

const AppInfo = {
  appName: 'EtchMarket APP',  // 应用信息（RainbowKit 弹窗里显示）
};

// ★ 配置钱包连接器（告诉 RainbowKit 支持哪些钱包）
const connectors = connectorsForWallets([
  {
    groupName: 'Recommended',  // 钱包分组名称（在弹窗里显示为"推荐"）
    wallets: [
      okxWallet({ projectId, chains }),           // OKX 钱包（排第一，针对国内用户）
      tokenPocketWallet({ projectId, chains }),   // TokenPocket
      injectedWallet({ chains }),                 // 浏览器注入的任意钱包
      rainbowWallet({ projectId, chains }),       // Rainbow
      metaMaskWallet({ projectId, chains }),      // MetaMask（最常用）
      walletConnectWallet({ projectId, chains }), // 扫码连接
      gateWallet({ chains }),                     // Gate.io 自定义钱包
    ],
  },
]);

// ★ 创建 wagmi 配置（Web3 连接的核心配置）
const wagmiConfig = createConfig({
  autoConnect: true,            // 页面刷新时自动重连上次使用的钱包
  connectors,                   // 使用上面配置的钱包连接器
  publicClient,                 // HTTP RPC 客户端
  webSocketPublicClient,        // WebSocket RPC 客户端
});

// ★ 自定义 RainbowKit 主题（深色主题 + 品牌色）
const myCustomTheme: Theme = merge(darkTheme(), {
  colors: {
    accentColor: '#D5E970',           // 强调色（黄绿色，与品牌一致）
    accentColorForeground: '#171A1F', // 强调色上的文字颜色（深色，保证可读性）
  },
} as Theme);
// ↑ 使用 lodash.merge 深度合并：在默认深色主题基础上只改颜色

// 创建 react-query 的 QueryClient 实例
// 这是整个应用的数据缓存中心
const queryClient = new QueryClient();

// ★ Providers 主组件
export function Providers({ children }: { children: React.ReactNode }) {
  const [mounted, setMounted] = useState(false);
  // ↑ 是否已经挂载（在浏览器中渲染完成）
  // 原因：服务端渲染时 window/localStorage 不存在，
  //       某些 Web3 操作只能在浏览器中执行
  //       mounted 为 true 才渲染需要浏览器环境的内容

  const globalStore = useSnapshot(GlobalStore.store);
  // ↑ 订阅全局状态（登录状态等）
  // 当 GlobalStore.store 变化时，这个组件会重新渲染

  useEffect(() => {
    setMounted(true);
    // ↑ 组件挂载后（第一次渲染到 DOM 之后）设置 mounted = true
    // [] 空依赖数组 = 只在组件首次挂载时执行一次
  }, []);

  return (
    <QueryClientProvider client={queryClient}>
    {/* ↑ 第1层：注入 react-query，所有子组件可以使用 useQuery */}

      <ThemeProvider theme={customTheme}>
      {/* ↑ 第2层：注入 MUI 主题，所有 MUI 组件会使用这个主题的颜色/字体 */}

        <CssBaseline />
        {/* ↑ 重置浏览器默认样式（不同浏览器有不同默认样式，这个组件统一抹平） */}

        <Toaster
          position="top-right"          // Toast 出现在右上角
          containerStyle={{
            top: 80,    // 距顶部 80px（让开 Header 的 64px 高度）
            right: 20,
          }}
          toastOptions={{
            success: {
              // ↑ 后面的代码被截断，这里配置成功 Toast 的样式
```

---

### 8.5 `src/stores/GlobalStore.ts` — 全局状态管理

```typescript
// ============================================================
// 文件说明：全局状态管理文件
// 使用库：valtio（比 Redux 简单很多的状态管理库）
//
// 什么是状态管理？
// 想象一下：用户登录后，网站的很多地方（Header、个人中心、购买按钮等）
// 都需要知道"用户已登录"这个信息。如果每个组件都自己管理，会很混乱。
// 状态管理就是把这些"全局共享的数据"放在一个地方统一管理。
//
// 为什么用 valtio 而不是 Redux？
// Redux：需要 action、reducer、dispatch 等概念，学习成本高
// valtio：直接修改对象就行，像操作普通变量一样简单
// ============================================================

import { AuthenticationStatus } from '@rainbow-me/rainbowkit';
// ↑ RainbowKit 定义的认证状态类型
// 可能的值：'authenticated'（已认证）| 'unauthenticated'（未认证）| 'loading'（加载中）

import { ValueOf } from 'next/dist/shared/lib/constants';
// ↑ TypeScript 工具类型，用于获取对象值的类型

import { proxy } from 'valtio';
// ↑ valtio 的核心函数：把普通对象变成"响应式代理对象"
// 修改 proxy 对象时，所有用 useSnapshot 订阅它的组件会自动更新

// 定义全局状态的类型（TypeScript 接口）
export interface IGlobalStore {
  rainbowKitAuthStatus: AuthenticationStatus; // 钱包认证状态
  isOgPass: boolean; // 是否持有 OG Pass 资格
}

// 定义 localStorage 中存储 token 的数据结构
// 格式：[{ "0xabc...": { token: "eyJ..." } }, ...]
// 用数组存多个地址的 token，支持多钱包地址切换
type AccessTokenType = {
  [key: string]: { token: string }; // key 是钱包地址，value 是 token 对象
};

// ★ 创建全局状态实例（valtio 的 proxy）
export const store = proxy<IGlobalStore>({
  rainbowKitAuthStatus: 'unauthenticated', // 默认未登录
  isOgPass: false, // 默认没有 OG Pass
});

// ★ 初始化登录状态（页面刷新时调用）
// 作用：从 localStorage 读取上次保存的 token，如果有效就自动恢复登录状态
export const initRainbowKitAuthStatus = (address: string) => {
  // 从 localStorage 读取所有地址的 token
  // ?? '[]' 是空值合并运算符：如果 localStorage 中没有这个值，就用 '[]'（空数组）
  const accessTokens: AccessTokenType[] = JSON.parse(localStorage.getItem('accessTokens') ?? '[]');

  // 找到当前钱包地址对应的 token
  // find 遍历数组，找到第一个包含当前地址的元素
  // ?. 是可选链操作符：如果 find 返回 undefined，不会报错，直接返回 undefined
  // ?? 如果找不到，使用默认值 { token: '' }
  const accessToken: ValueOf<AccessTokenType> = accessTokens.find((item) => item[address])?.[address] ?? { token: '' };

  if (accessToken?.token.trim() !== '') {
    // ↑ 如果 token 不为空（trim() 去除首尾空格后）
    localStorage.setItem('token', accessToken?.token);
    // ↑ 把 token 单独存一份（方便 axios 拦截器读取）
    store.rainbowKitAuthStatus = 'authenticated';
    // ↑ 直接修改 proxy 对象 = valtio 的核心用法
    // 修改后，所有订阅这个 store 的组件会自动更新
  } else {
    localStorage.removeItem('token'); // 清除无效 token
    store.rainbowKitAuthStatus = 'unauthenticated';
  }
};

// ★ 设置登录/登出状态
// 参数：
//   status - 新的认证状态
//   address - 钱包地址
//   token - JWT 令牌
export const setRainbowKitAuthStatus = (status: AuthenticationStatus, address: string, token: string) => {
  let accessTokens: AccessTokenType[] = JSON.parse(localStorage.getItem('accessTokens') ?? '[]');

  if (status === 'authenticated') {
    // 登录成功：保存新 token
    accessTokens.push({ [address]: { token } });
    // ↑ [address] 是"计算属性名"语法，key 是变量 address 的值

    localStorage.setItem('token', token);
    // ↑ 保存 token（供 axios 拦截器读取）

    localStorage.setItem('accessTokens', JSON.stringify(accessTokens));
    // ↑ 把整个 token 列表序列化为 JSON 字符串存入 localStorage
    // JSON.stringify：对象 → JSON 字符串（存储）
    // JSON.parse：JSON 字符串 → 对象（读取）
  } else if (status === 'unauthenticated') {
    // 登出：清除 token
    localStorage.removeItem('token');

    // 把该地址的 token 置为空（而不是删除记录，保留历史）
    accessTokens = accessTokens.map((item) => {
      return item[address] ? { [address]: { token: '' } } : item;
      // ↑ 找到该地址的记录，把 token 清空；其他地址的记录不动
    });

    localStorage.setItem('accessTokens', JSON.stringify(accessTokens));
  }

  store.rainbowKitAuthStatus = status;
  // ↑ 更新全局状态（触发订阅该 store 的组件重新渲染）
};

// 设置 OG Pass 资格
export const setIsOGPass = (flag: boolean) => {
  store.isOgPass = flag;
  // ↑ 直接修改，简单直接，这就是 valtio 的优雅之处
};
```

---

### 8.6 `src/services/network/etchClient.ts` — HTTP 客户端（含认证）

```typescript
// ============================================================
// 文件说明：封装好的 HTTP 请求客户端
// 作用：
//   1. 配置 API 基础地址
//   2. 自动在请求头加上 JWT token（认证）
//   3. 处理 401 错误（token 过期）
//
// 什么是拦截器？
// 类比：快递中转站。请求拦截器在"发出"前处理，响应拦截器在"收到"后处理
// 用途：统一加请求头、统一处理错误、添加 loading 状态等
// ============================================================

import { SERVER_CONFIG } from '@/constants/config';
// ↑ 导入环境配置（BASE_URL 是 '/api' 或 '/api-goerli'）

import ApiClient from './ApiClient';
// ↑ 基础 API 客户端类（封装了 get/post/put/delete 方法）

import axios from 'axios';
// ↑ HTTP 请求库

// ★ 创建 axios 实例（配置默认值）
const etchClient = new ApiClient(
  axios.create({
    baseURL: SERVER_CONFIG.BASE_URL, // 所有请求的基础 URL 前缀
    // 例如：get('/ethscriptions/latest') 实际请求 '/api/ethscriptions/latest'

    responseType: 'json' as const, // 响应数据类型（自动解析 JSON）

    headers: {
      'Content-Type': 'application/json', // 告诉服务器：我发的是 JSON 格式
    },

    timeout: 10 * 1000, // 超时时间：10秒（10 * 1000 毫秒）
    // 如果 10 秒内没收到响应，请求失败
  }),
);

// ★ 请求拦截器（每次发请求前自动执行）
etchClient.client.interceptors.request.use(
  (config) => {
    // 如果 localStorage 中有 token（用户已登录）
    if (localStorage.getItem('token')) {
      config.headers['Authorization'] = `Bearer ${localStorage.getItem('token')}` ?? 'Bearer ';
      // ↑ 在请求头中加入认证信息
      // Bearer Token 是 JWT 认证的标准格式
      // 服务器收到请求后，会解析这个 token，确认请求者的身份
    }
    return config; // 必须返回 config，请求才会继续发出
  },
  (error) => {
    return Promise.reject(error); // 如果拦截器本身出错，向下传递错误
  },
);

// ★ 响应拦截器（每次收到响应后自动执行）
etchClient.client.interceptors.response.use(
  (response) => {
    return response; // 正常响应：直接透传
  },
  (error) => {
    if ([401].includes(error.response.status)) {
      // ↑ 如果服务器返回 401（未授权 / token 失效）
      localStorage.removeItem('token');
      // ↑ 清除本地 token（下次请求不会再带上过期的 token）
      return;
      // ↑ 静默处理：不向上抛出错误（不会触发业务代码的 catch）
      // 可能是设计选择：401 时不报错，让页面正常显示但不含需要登录的数据
    }
    return Promise.reject(error);
    // ↑ 其他错误（500、404 等）：向上抛出，让业务代码处理
  },
);

export default etchClient;
// ↑ 导出这个配置好的客户端实例，供 services 层使用
```

---

## 九、如果你是新手，怎么从零理解这个项目？

### 9.1 前置知识准备清单

在阅读这个项目之前，建议你具备以下基础：

**必须掌握（基础）：**

- [ ] HTML/CSS 基础
- [ ] JavaScript ES6+（箭头函数、解构赋值、async/await、Promise）
- [ ] React 基础（JSX、组件、props、state、useEffect、useState）
- [ ] TypeScript 基础（类型注解、接口、泛型）

**有助于理解（进阶）：**

- [ ] Next.js 基础（App Router 路由、Server/Client Component 区别）
- [ ] MUI 组件库的基本用法
- [ ] HTTP 请求基础（GET/POST、请求头、响应码）
- [ ] REST API 概念（endpoint、query params）

**Web3 部分（可边学边看）：**

- [ ] 区块链基本概念（钱包地址、私钥、交易、Gas）
- [ ] 以太坊基础（什么是主网/测试网、什么是智能合约）
- [ ] 什么是 NFT

### 9.2 分阶段学习路线

**第一阶段（1-2天）：了解项目全貌**

1. 在本地跑起来（`npm install` → `npm run dev`）
2. 浏览器访问 `http://localhost:3000`，点击所有菜单，感受功能
3. 阅读本文档的第一到七节

**第二阶段（2-3天）：理解代码骨架**

1. 读 `layout.tsx`，理解 Next.js 应用的外壳
2. 读 `providers.tsx`，理解全局配置的层层嵌套
3. 读 `Header.tsx`，理解一个真实容器组件是什么样的
4. 读 `GlobalStore.ts`，理解 valtio 状态管理

**第三阶段（3-5天）：深入一个完整功能**

1. 以"市场页"为切入点：
   - `src/app/market/page.tsx`（页面入口）
   - `src/containers/CollectionList/`（数据容器）
   - `src/services/marketpalce/`（API 层）
2. 跟踪数据从 API 到页面的完整流向

**第四阶段（持续）：理解 Web3 部分**

1. 读 `authenticationAdapter.ts`，理解 Web3 登录
2. 读 `constants/abis.ts`，理解智能合约交互
3. 读 `services/evm/`，理解链上数据读取

### 9.3 调试技巧

1. **在浏览器 DevTools 看网络请求**：Network 标签页，看 `/api/*` 的请求和响应
2. **在 React DevTools 看组件树**：浏览器扩展，看 Provider 是怎么层层嵌套的
3. **在代码里加 console.log**：临时打印数据，理解数据结构
4. **用 Valtio DevTools**：可以看全局状态的变化历史

---

## 十、第二批文件解读（Header / CartStore / hooks / ethscriptions服务层）

### 10.1 `src/containers/Header.tsx` — 顶部导航栏深度解读

Header 是整个项目里**响应式设计最典型**的组件，值得单独深入理解。

#### 响应式三种状态对照表

| 屏幕宽度                  | Logo     | 搜索框         | 导航菜单 | 钱包按钮 | 购物车 |
| ------------------------- | -------- | -------------- | -------- | -------- | ------ |
| ≥ 1250px（宽屏PC）        | 完整Logo | Header内搜索框 | 横向菜单 | ✅显示   | ✅显示 |
| 750px-1250px（窄PC/平板） | 完整Logo | 无             | 横向菜单 | ✅显示   | ✅显示 |
| < 750px 首页              | 完整Logo | 无             | 汉堡菜单 | 汉堡内   | ✅显示 |
| < 750px 非首页            | 简化Logo | Logo右侧搜索   | 汉堡菜单 | 汉堡内   | ✅显示 |

#### 核心逻辑：`useMediaQuery` 的多断点组合

```typescript
const matches = useMediaQuery('(min-width:750px)'); // 是否 PC/平板
const hidePcSearch = useMediaQuery('(max-width:1250px)'); // 是否隐藏PC搜索框

// 复合条件：不是首页 AND 屏幕足够宽 → 显示Header内搜索框
const showPcSearch = useMemo(() => !isHome && !hidePcSearch, [pathname, hidePcSearch]);
```

**为什么用 `useMemo`？**
每次组件渲染都会重新计算 `showPcSearch`。如果直接写 `const showPcSearch = !isHome && !hidePcSearch`，每次渲染都是一次新的计算。虽然这个计算很简单，但 `useMemo` 是良好编码习惯，在复杂计算时可以防止性能问题。

#### 登录状态初始化的触发时机

```typescript
useEffect(() => {
  if (address) {
    GlobalStore.initRainbowKitAuthStatus(address);
    // 关键：每次地址变化（包括首次连接）都会尝试从 localStorage 恢复 token
  }
}, [address, connector]);
```

这个设计确保了：用户刷新页面后，只要钱包还连着，登录状态就会自动恢复。

---

### 10.2 `src/stores/CartStore.ts` — 购物车状态深度解读

#### valtio `subscribe` 的自动持久化机制

```typescript
subscribe(store.orderIds, () => {
  localStorage.setItem('sweepOrderIds', JSON.stringify(store.orderIds));
});
```

这行代码实现了一个**响应式持久化**：

- `subscribe(target, callback)`：当 `target`（这里是 `store.orderIds` 数组）发生任何变化时，自动执行 `callback`
- `callback` 里把最新状态写入 `localStorage`
- 结果：购物车的增删操作会"实时自动存档"，用户刷新页面不丢失

**为什么 `clearAllEthsciption` 用 `splice` 而不是 `= []`？**

```typescript
// ❌ 错误做法（subscribe 无法检测到）
store.orderIds = [];

// ✅ 正确做法（in-place 修改，subscribe 能检测到）
store.orderIds.splice(0, store.orderIds.length);
```

这是 valtio 使用的关键细节：`subscribe` 订阅的是**对象引用**，如果你把 `store.orderIds` 替换成一个全新的数组，`subscribe` 监听的还是旧数组对象，触发不了回调。`splice` 就地修改同一个数组，valtio 的 Proxy 能检测到。

#### 打开购物车时的"订单验证"逻辑

```typescript
// signature 为空 = 订单无效（已成交或已撤销）
const _orderIds = _cartOrder.orders.filter((item) => item.order.signature == '');
_orderIds.forEach((item) => {
  removeEthscription(item.order.orderHash); // 自动清理无效订单
});
```

**为什么这么设计？**
用户可能在A设备上把 NFT 加入购物车，然后在B设备上完成购买。当再次打开购物车时，后端数据库已经把那条挂单的签名清空（表示已成交），前端检测到这种情况就自动移除该商品，防止用户重复支付。

---

### 10.3 `src/hooks/` — 自定义 Hook 深度解读

#### Hook 的本质：复用"有状态的逻辑"

React 组件复用的是 UI，而 Hook 复用的是逻辑（包含状态和副作用）。

**类比：**

- 组件 = 复用 UI 外观（就像装修风格）
- Hook = 复用逻辑行为（就像家电的工作原理）

#### `useEthersProvider` vs `useEthersSigner` 的区别

```
useEthersProvider（只读）    useEthersSigner（可写）
       ↓                            ↓
  获取余额、读合约状态          发送交易、调用合约写方法
  任何人都能用                  必须已连接钱包
  对应 viem PublicClient        对应 viem WalletClient
```

**实际使用场景：**

```typescript
// 读合约（不需要签名）→ 用 Provider
const provider = useEthersProvider();
const balance = await provider.getBalance(address);

// 写合约（需要签名）→ 用 Signer
const signer = useEthersSigner();
const contract = MyContract__factory.connect(contractAddress, signer);
await contract.purchase(tokenId, { value: price });
```

#### 为什么 wagmi v1 + ethers.js 需要这两个转换 Hook？

这是**历史债务**问题：

1. wagmi v0.x：内部使用 ethers.js，直接提供 ethers.js 的 Provider 和 Signer
2. wagmi v1.x：重构为使用 viem（更现代、性能更好），提供 viem 的 PublicClient 和 WalletClient
3. 但项目中合约相关的代码（typechain 生成的类型、某些直接操作）还是用 ethers.js 写的
4. 解决方案：写两个转换 Hook，做 viem → ethers 的适配

---

### 10.4 `src/services/ethscriptions/index.ts` — 服务层深度解读

#### 为什么用 Class 而不是普通函数？

```typescript
// 方案A：Class（当前做法）
class EthscriptionsService {
  private apiClient: ApiClient;  // 私有依赖
  constructor(apiClient: ApiClient) { this.apiClient = apiClient; }
  async getLatestEths(data) { ... }
}
export default new EthscriptionsService(etchClient);

// 方案B：普通函数
export async function getLatestEths(data) {
  return etchClient.get(`/ethscriptions/latest?${...}`);
}
```

**Class 的优势：**

1. **依赖注入**：测试时可以传入 mock 的 apiClient，不需要真实网络请求
2. **单一实例**：`new EthscriptionsService(etchClient)` 只创建一次，整个应用复用
3. **扩展性**：可以在构造函数里做更多初始化，方便后续扩展

#### `getAssetList` 的多阶段并发请求解析

```
阶段1（串行）：获取集合详情（需要其中的 Facet 路由地址才能进行下一步）
      ↓
阶段2（并发）：同时发出 2 个请求
  ├─ getCollectionOwnerList（以太坊链资产）
  └─ getPairs（Facet 链资产）
      ↓
阶段3（串行）：根据阶段2的结果，获取价格信息
      ↓
阶段4（数据整合）：合并两个来源的资产数据，计算美元价值
```

**Promise.all 的原理：**

```typescript
// 串行写法（慢，2+1=3秒）
const result1 = await request1(); // 等2秒
const result2 = await request2(); // 再等1秒

// 并行写法（快，max(2,1)=2秒）
const [result1, result2] = await Promise.all([request1(), request2()]);
```

`Promise.all` 同时启动所有 Promise，等所有都完成后才继续。如果任何一个失败，整个 `Promise.all` 立即失败。

---

### 10.5 首页（`home/index.tsx` + `HomeLayout.tsx`）架构解析

#### 组件层次结构

```
RootLayout（layout.tsx）
└── Providers（providers.tsx）
    ├── Header（containers/Header.tsx）
    └── ProvidersMiddleware（providersMiddleware.tsx）
        └── Home（app/home/index.tsx）← 当前分析的文件
            └── HomeLayout（app/home/HomeLayout.tsx）
                ├── 内容区
                │   ├── 标题
                │   ├── MutipleTypeSearchInput（搜索框）
                │   ├── RecentEthsciptionList（最新铭文）
                │   └── LatestTransactionList（最新交易）
                └── Footer（容器组件）
```

#### 搜索的"受控组件"模式

```typescript
// 状态由父组件（Home）持有
const [searchType, setSearchType] = useState<SearchType>('all');
const [searchBy, setSearchBy] = useState('');

// 传给子组件（MutipleTypeSearchInput）
<MutipleTypeSearchInput
  searchType={searchType}           // 把状态传下去（props down）
  searchBy={searchBy}               // 把状态传下去
  onSearchTypeChage={setSearchType} // 把修改状态的函数传下去（events up）
  onSearchByChange={setSearchBy}    // 事件回调
/>
```

**受控组件的好处：**

- 父组件完全知道子组件的当前状态
- 可以在多个地方使用相同的状态（如搜索类型也影响搜索结果页的展示）
- 便于在 React DevTools 中调试

#### `HomeLayout` 的 CSS 背景固定技巧

```css
background: url('/images/home_bg.webp');
backgroundattachment: 'fixed'; /* 关键：背景图固定不动 */
backgroundsize: 'cover';
```

这产生了**视差滚动效果**：页面内容会滚动，但背景图保持固定，创造出内容"浮在背景上"的层次感。

**注意事项：**
`backgroundAttachment: fixed` 在移动端 iOS Safari 上有兼容性问题，可能导致性能下降或显示异常。这是项目的一个潜在优化点。

---

### 10.6 代码中常见的 TypeScript 技巧总结

通过前面的代码分析，整理出这个项目中频繁出现的 TypeScript 技巧：

#### 可选链（Optional Chaining）`?.`

```typescript
response?.code; // 如果 response 是 null/undefined，不报错，返回 undefined
store.cartOrder?.orders; // 如果 cartOrder 是 null，不报错
```

#### 空值合并（Nullish Coalescing）`??`

```typescript
localStorage.getItem('token') ?? ''; // 如果是 null/undefined，用 ''
accessToken ?? { token: '' }; // 提供默认值
```

#### 解构赋值 + 剩余语法

```typescript
const { address, ...props } = data;
// address 单独取出，剩余所有属性收集到 props 对象
```

#### 计算属性名

```typescript
const address = '0xabc';
const obj = { [address]: { token: 'xxx' } };
// 等价于 { '0xabc': { token: 'xxx' } }
```

#### 类型断言 `as`

```typescript
getQueryParams(data as any); // 跳过类型检查（应谨慎使用）
```

#### 泛型 `<T>`

```typescript
async get<TResponse>(path: string): Promise<TResponse>
// 调用时：get<UserInfo>('/user') → 返回值被推断为 UserInfo 类型
```

---

## 十一、第三批文件解读（智能合约 ABI / 市场模块 / 批量操作 / 主题）

### 11.1 `src/constants/abis.ts` — 智能合约 ABI 解读

ABI（Application Binary Interface，应用二进制接口）是前端与智能合约通信的"翻译手册"。本文件包含 **6 个核心合约的 ABI**：

| #   | 合约名                  | 功能                        | 关键方法                                                |
| --- | ----------------------- | --------------------------- | ------------------------------------------------------- |
| 1   | `EtchMarketABI`         | 主市场合约（单个 NFT 交易） | `executeEthscriptionOrder`、`cancelMultipleMakerOrders` |
| 2   | `EtchMarketSweepABI`    | 扫货合约（批量购买）        | `sweepCollection`                                       |
| 3   | `EtchLaunchpadABI`      | 发射台合约                  | `mint`、`setMintPrice`                                  |
| 4   | `EtchMarketVaultABI`    | 金库合约（NFT 托管）        | `deposit`、`withdraw`、`transferEthscription`           |
| 5   | `ERC20SFactoryABI`      | 铭文代币工厂                | `deploy`、`getToken`                                    |
| 6   | `ERC20EthscriptionsABI` | 铭文代币标准接口            | `balanceOf`、`transfer`、`approve`                      |

#### ABI 的三种成员类型对照

```typescript
// ① error：合约抛出的自定义错误（比 require 字符串省 Gas）
{ inputs: [], name: 'OrderExpired', type: 'error' }
// 触发场景：购买已过期订单时合约 revert

// ② event：合约发出的事件（前端通过监听获取链上变化）
{ name: 'EthscriptionOrderExecuted', type: 'event', inputs: [...] }
// 触发场景：交易成功后，前端可以监听这个事件来更新 UI

// ③ function：可调用的合约函数
{ name: 'executeEthscriptionOrder', type: 'function',
  inputs: [...], stateMutability: 'payable' }
// stateMutability:
//   - view：只读（不花 Gas）
//   - pure：纯函数（不读写状态）
//   - nonpayable：可写但不接收 ETH
//   - payable：可接收 ETH（如购买函数）
```

#### 一次完整的购买流程涉及哪些 ABI 调用

```
用户点击 "Buy" 按钮
  ↓
前端：构造订单数据 + 调用 EtchMarketABI.executeEthscriptionOrder(order, recipient, '0x')
  ↓
钱包弹窗显示支付 ETH 金额（msg.value = order.price）
  ↓
用户确认 → 交易广播到以太坊网络
  ↓
合约执行：
  1. 验证签名（卖家签名是否有效）→ 失败抛 SignatureInvalid
  2. 检查订单是否过期 → 失败抛 OrderExpired
  3. 调用 Vault.transferEthscription(铭文, 买家)
  4. 把 ETH 转给卖家（扣除协议费/创作者费）
  5. 触发 EthscriptionOrderExecuted 事件
  ↓
前端监听到 EthscriptionOrderExecuted → 刷新 UI、显示成功提示
```

#### 为什么需要 6 个合约而不是一个？

这是**模块化设计**：

- **解耦**：市场合约不需要知道金库的内部实现，只调用接口
- **可升级**：单个合约出 bug 只需要升级该合约，不影响其他模块
- **省 Gas**：每个合约职责单一，代码量少，部署成本低

---

### 11.2 `src/constants/theme.ts` — MUI 主题配置解读

主题文件虽然只有 30 行，但定义了**整站设计语言**。

#### MUI 主题的 4 个核心概念

```typescript
// 1. breakpoints（响应式断点）
//    决定 xs/sm/md/lg/xl 对应的屏幕宽度
//    使用：sx={{ display: { xs: 'none', md: 'flex' } }}

// 2. palette（调色板）
//    mode: 'dark'   → 深色模式（背景暗、文字亮）
//    primary.main   → 品牌主色（按钮、高亮）
//    background.default → 全局背景色

// 3. typography（排版）
//    fontFamily → 全局字体

// 4. components（组件默认属性）
//    可以覆盖 MUI 组件的默认行为
//    例：禁用所有按钮的水波纹效果
```

#### 颜色密码：项目的视觉识别

- **主色 `#E5FF65`**（荧光黄绿）→ 所有 CTA 按钮、链接高亮、品牌 Logo
- **背景 `#171A1F`**（深近黑）→ 全局背景，营造 Web3 科技感
- **卡片背景 `#202229`**（深灰）→ 卡片、表格等次级容器
- **边框 `#2F343E`**（中灰）→ 分隔线、卡片边框

看到代码里出现 `#E5FF65` 就知道是品牌主色，看到 `#202229` 就知道是卡片背景。

#### 为什么禁用水波纹（disableRipple）？

MUI 默认按钮点击有水波纹动画（Material Design 的标志）。EtchMarket 追求**极简 Web3 风格**，水波纹会让 UI 过于"安卓化"，与整体设计语言冲突。所以全局禁用。

---

### 11.3 `src/services/marketpalce/index.ts` — 市场服务深度解读

#### 接口分组速查表

| 分组      | 方法                        | 场景           |
| --------- | --------------------------- | -------------- |
| 列表查询  | `getMarketList`             | 市场列表页     |
|           | `getMyEthscriptions`        | 我的资产页     |
|           | `getCollectionList`         | 市场首页大表   |
|           | `getCollectionOwnerList`    | 筛选某集合资产 |
|           | `getCollectionDetail`       | 集合详情页头部 |
| 挂单操作  | `addListing`                | 单个上架       |
|           | `addListingBulk`            | 批量上架       |
|           | `editListing`               | 修改挂单价     |
| 撤销/提取 | `getWithdraw`               | 单个撤销       |
|           | `getWithdrawBulk`           | 批量撤销       |
| 购物车    | `getOrderID`                | 查单订单       |
|           | `getOrderIDByCart`          | 批量验证订单   |
|           | `getOrderNonce`             | 获取下单 nonce |
| 统计/价格 | `getEthPrice`               | ETH 美元价格   |
|           | `marketChart`               | 价格图表数据   |
| 历史交易  | `getHistoryOrder`           | 成交历史       |
| 铭文详情  | `getEtherscriptionAsset`    | 铭文资产信息   |
|           | `getEtherscriptionActivity` | 铭文活动历史   |
| Swap      | `getSwapActivity`           | 兑换历史       |
|           | `getSwapTokensInfo`         | 兑换代币信息   |

#### 设计亮点：购物车订单验证（`getOrderIDByCart`）

```typescript
// 后端返回所有订单，前端做有效性筛选
if (item.order.signature.trim() == '') {
  invalidList.push(item); // 签名为空 = 已成交/已撤销
} else {
  efficientList.push(item);
}
response.data.orders = efficientList.concat(invalidList);
// 重排序：有效在前，无效在后
```

**为什么 signature 为空表示订单失效？**
后端在订单成交或撤销后，会清空 signature 字段。前端以此判断订单是否还能购买。这样设计的好处：

1. **一致性**：所有失效订单都用同一个字段标记
2. **效率**：不需要额外查询数据库状态
3. **用户体验**：失效订单仍然显示（标灰），让用户知道发生了什么

#### URL 设计哲学

```typescript
// 模式1：资源 ID 在路径里
GET /markets/order/{orderId}              ← 查单订单
GET /markets/ethscriptions/address/{owner} ← 查某地址资产

// 模式2：复杂参数在 body 里（POST）
POST /markets/cart/orders                  ← 批量查询订单
POST /markets/ethscriptions/withdraw/bulk  ← 批量撤销

// 模式3：过滤条件在 query string 里
GET /markets/collections?category=token&page.size=50
```

**选择规则：**

- 单个资源标识 → 路径
- 长数组、复杂对象 → POST body
- 简单过滤条件、分页 → query string

---

### 11.4 `src/stores/BulkListingStore.ts` — 批量操作 Store 深度解读

#### 核心数据结构：5 个列表的关系

```
ethsList（总列表）
   │
   ├── 是否 isVerified？
   │     ├── 是 → 根据状态进入有效操作列表
   │     │       ├── isListing=false → bulkListEths（可上架）
   │     │       ├── isListing=true  → bulkUnlistEths（可下架）
   │     │       └── owner=protocol  → bulkWithdrawEths（可提取）
   │     │
   │     └── 否 → 仅可转账（如果是自己的）
   │              └── owner=me → bulkTransferEths
```

**为什么一个铭文要同时在多个列表里？**
因为不同列表对应不同的操作面板。用户切换面板时不需要重新计算分类，直接从对应列表读取即可。这是**用空间换时间**的设计。

#### 关键陷阱：`splice` vs `= []`

```typescript
// ❌ 错误（valtio subscribe 检测不到）
store.ethsList = [];

// ✅ 正确（valtio 能监听到）
store.ethsList.splice(0, store.ethsList.length);
```

**原理：** valtio 通过 Proxy 监听对象变化。`splice` 是修改同一个数组对象，Proxy 能拦截；而 `= []` 是赋值新数组，Proxy 监听的还是旧数组对象，无法触发响应。

#### `ethers.utils.getAddress` 的作用

```typescript
ethers.utils.getAddress(eths.order.owner) == ethers.utils.getAddress(wagmiStore.state.data.account);
```

以太坊地址有两种格式：

- 全小写：`0xabcdef1234...`
- Checksum：`0xAbCdEf1234...`（特定字符大写）

直接比较字符串可能出现"两个地址实际相同但字符串不等"的问题。`getAddress()` 会把任何格式转为标准 Checksum 格式，确保比较准确。

---

### 11.5 市场模块（`app/market/page.tsx` + `CollectionList/index.tsx`）解读

#### 页面分层：薄页面 + 厚容器

```typescript
// page.tsx（极简，只有10行）
return <Box><CollectionList /></Box>;

// CollectionList/index.tsx（厚重，含所有业务逻辑）
- useSearchParams 读取 URL 参数
- useImmer 管理过滤状态
- useDebounce 防抖
- useEffect 调用 API
- 渲染搜索框 + TableData
```

**为什么这样分层？**

- **页面文件**：负责路由约束（Next.js 要求每个路由有 `page.tsx`）
- **容器组件**：负责业务逻辑，可以被其他地方复用（虽然目前没复用）
- **展示组件**（TableData）：负责纯渲染，可以独立测试

#### `useImmer` 的甜蜜陷阱

```typescript
// useState 写法（繁琐）
setFilterRequest({ ...filterRequest, category: 'token' });
setFilterRequest({ ...filterRequest, 'page.index': 1, category: 'token' });

// useImmer 写法（直观）
setFilterRequest((state) => {
  state.category = 'token';
  state['page.index'] = 1;
});
```

`useImmer` 用 immer 库的 produce 函数，让你**像修改普通对象一样**修改状态，内部自动生成新对象（保持不可变性）。

**注意：** useImmer 的 setter 函数返回 void，不要在里面写 `return state.xxx`，会导致状态被设置为单个值。

#### 防抖（useDebounce）的实际效果

```
用户输入：c → ca → cat → cats
  ↓ 不用防抖：发 4 次请求
  ↓ 用防抖（300ms）：只发 1 次请求（"cats"）
```

```typescript
const filterRequestDebounce = useDebounce(filterRequest, 300);
useEffect(() => {
  getCollectionList();
}, [filterRequestDebounce]);
```

**关键：** useEffect 监听的是**防抖后**的值，不是原始值。所以原始值频繁变化不会触发 effect，只有 300ms 内没变化才会触发。

#### `useRef` vs `useState` 的取舍

搜索框这里用了 `useRef(inputRef)` 而不是 `useState`：

| 场景                   | 用什么   | 原因                 |
| ---------------------- | -------- | -------------------- |
| 每次输入都需要 UI 反应 | useState | 触发重渲染           |
| 只在某个时刻读取值     | useRef   | 不触发重渲染，性能好 |

这里只在"按回车"或"点搜索按钮"时才读取值，不需要每次按键都重渲染整个组件，所以用 useRef 更合适。

---

### 11.6 第三批跨文件知识总结

#### Valtio 状态管理的 3 大规则

1. **修改用 splice，不用赋值**：保持引用不变才能触发响应
2. **subscribe 是自动持久化的最简方案**：监听整个 store，自动写 localStorage
3. **多个 store 按业务领域拆分**：CartStore（购物车）、BulkListingStore（批量操作）、GlobalStore（全局登录），各自独立

#### Service 类的命名约定

| 方法前缀            | 含义         | 示例                                            |
| ------------------- | ------------ | ----------------------------------------------- |
| `get`               | 查询数据     | `getCollectionList`                             |
| `add`               | 创建新记录   | `addListing`                                    |
| `edit`              | 修改现有记录 | `editListing`                                   |
| `remove` / `delete` | 删除         | 此项目用 cancelMultipleMakerOrders 在合约层完成 |

---

## 十二、第四批文件解读（服务层补全 / 购物车 UI / 导航 / 搜索）

### 12.1 `services/launchpad/index.ts` — 发射台服务

**职责：** 包装 2 个简单接口给 UI 调用：

| 方法               | 用途                         | URL 模式                             |
| ------------------ | ---------------------------- | ------------------------------------ |
| `getLaunchpadList` | 列出所有发射台项目（首页用） | `GET /launchpad/list?page=1&size=20` |
| `getLaunchpadInfo` | 单个项目详情（详情页用）     | `GET /launchpad/get/{name}?lang=en`  |

**什么是 Launchpad？**
Web3 里的"众筹首发平台"。项目方上线即将开放铸造的项目，用户参与铸造（mint）。

**亮点：路径参数 + Query 参数混用**

```typescript
const { name, ...props } = data;
const queryParams = getQueryParams(props as any);
return await this.apiClient.get(`/launchpad/get/${name}?${queryParams}`);
```

- `name`（资源标识）→ 放路径 → URL 语义清晰、便于 SEO
- 其他可选参数 → 放 query string → 不影响资源定位

---

### 12.2 `services/did/index.ts` — DID 身份认证服务

**什么是 DID（Decentralized Identifier）？**
去中心化身份标识。在 Web3 里就是用"钱包地址 + 签名"当作身份证明，代替"用户名 + 密码"。

**完整登录流程（前端 ↔ 后端）：**

```
1. 用户点击 "Connect Wallet"     → 钱包弹窗 → 获得地址 0xABC...
2. 前端 getAuthNonce('0xABC')    → 后端返回 nonce="random_xyz"
3. 前端用钱包对 nonce 签名         → 得到 signature
4. 前端 authVerify(address, signature, nonce)
                                  → 后端验证签名是该地址私钥所签
                                  → 返回 JWT token
5. 前端把 token 存起来（cookie/localStorage），后续请求都带上
6. 前端 getUserInfo(address)      → 返回用户资料（昵称、头像等）
```

**为什么需要 nonce（一次性随机数）？**
防止"重放攻击"。如果不用 nonce，黑客窃取到签名后可以无限次重放该签名登录。每个 nonce 只能用一次，服务端验证后立即失效。

**历史命名注意：** 类名是 `MarketplaceService` 但实际职责是 DID 认证，不要被命名误导。

---

### 12.3 `services/network/facetClient.ts` — Facet L2 客户端

**Facet 是什么？**
建立在以太坊之上的 L2（Layer 2）协议，主要服务 Ethscriptions（铭文）。它有自己的区块浏览器（类 Etherscan），通过 SCAN_URL 提供 API。

**为什么单独建一个 client？**

- 不同后端服务有不同 BaseURL、不同认证方式、不同超时设置
- 每个外部 API 封装成独立 client 实例，便于统一管理 + 互不影响

**与 etchClient 对比：**

| Client        | 用途                | 认证           |
| ------------- | ------------------- | -------------- |
| `etchClient`  | 主后端 API          | 有 JWT 拦截器  |
| `facetClient` | Facet L2 区块浏览器 | 无（公开数据） |

---

### 12.4 `containers/CartDrawer/` — 购物车抽屉（5 文件套）

#### 文件结构与职责拆分

```
CartDrawer/
├─ index.tsx          → 总入口：图标 + 数字角标 + Drawer 容器
├─ CartOperation.tsx  → 顶部操作区：标题 + 数量 + 清空 + 关闭
├─ CartList.tsx       → 中间列表区：三列网格表格（图/数量/价格）
├─ CartSettlement.tsx → 底部结算区：总价 + Buy Now 一键购
└─ CartEmpty.tsx      → 空状态：插画 + "Explore Collections" CTA
```

**条件渲染逻辑：**

```typescript
{cartStore.orderIds.length ? (
  <Fragment>
    <CartList />
    <CartSettlement />
  </Fragment>
) : (
  <CartEmpty />
)}
```

#### 为什么用 Drawer 而不是 Modal？

- **Drawer**（抽屉）：不遮挡主页面左侧，可以一边看商品一边查看购物车
- **Modal**（弹窗）：会遮挡整个页面，体验不如 Drawer 流畅

#### 关键设计：失效订单视觉降级

```typescript
opacity: item.order.signature.trim() == '' ? 0.3 : 1;
```

后端在订单成交/撤销后清空 signature 字段。前端检测到 signature 为空就把整行变灰 + 价格列改为 "Unavailable"（红色）。这样：

- 用户能看到自己加入的所有项
- 一眼分辨哪些还能买、哪些已失效
- 不会"无脑点购买后才发现没货"

#### 一键扫货（Sweep）流程

```typescript
async function handleOnClickBuySweep() {
  setIsSubmit(true);
  try {
    // 关键：只把有效订单传给合约
    const efficientList = orders.filter((item) => item.signature.trim() !== '');
    const result = await evmService.etchMarketSweep.batchBuyEthscription({
      singer,
      ethscriptionSweep: { sweepAddress, orders: efficientList },
      sellPrice: accumlatePrice, // 用 BigNumber 累加的总价
    });
    // ...
  } finally {
    setIsSubmit(false);
    setOpenResult(true); // 无论成败都打开结果弹窗
  }
}
```

**关键点：**

1. `useEthersSigner()` 从 wagmi 转换出 ethers v5 Signer
2. `try-catch-finally` 模式：成败都要关闭 loading + 打开结果弹窗，避免状态残留
3. `BigNumber.reduce` 累加价格：wei 是大整数（10^18 量级），不能用 JS 原生 Number 累加

#### 为什么 `WalletConnectButton` 包裹 Buy Now？

这是**权限包裹模式**：

- 未连接钱包 → 点击子按钮被拦截 → 自动弹出钱包连接对话框
- 已连接钱包 → 子按钮正常触发 `handleOnClickBuySweep`

避免每个需要钱包的按钮都重复写"先检查登录"的样板代码。

---

### 12.5 `containers/Footer.tsx` — 页脚组件

**职责：**

1. 可选展示社交媒体图标列表
2. 展示商务联系入口

**设计思路：可控可复用**
通过 `showMedia: boolean` props 控制是否显示子块。同一个 Footer 组件适用多种场景（首页用全版、内嵌页用简版），避免写两个类似组件。

```typescript
<Footer />                  // 全版
<Footer showMedia={false} /> // 简版（不显示社交图标）
```

---

### 12.6 `containers/NavigationAPP.tsx` — 主导航菜单

#### 文件结构（3 个组件）

```
1. IndexerMenu  → "Indexer" 下拉菜单（含 Ethscriptions、Transactions 子项）
2. MoreMenu     → "..." 下拉菜单（含外链：表单、Dune 数据）
3. NavigationAPP → 主组件（拼装上述两个 + 平铺菜单项）
```

#### 响应式双形态

| 屏幕            | 菜单方向 | 子菜单实现            |
| --------------- | -------- | --------------------- |
| 桌面（>=750px） | 横向     | MUI `<Menu>` 下拉     |
| 移动（<750px）  | 纵向     | MUI `<Collapse>` 折叠 |

**关键：** 数据共享（同一份 indexerLinks/outerLinks 数组）+ UI 双套渲染。

#### 高亮规则的细节

```typescript
const isActive =
  item.pathname == '/'
    ? item.pathname == pathname // 首页：严格相等
    : pathname.startsWith(item.pathname); // 其他：起始匹配
```

**为什么首页要严格相等？**
因为所有路径都以 `/` 开头，如果用 startsWith 会导致首页菜单永远高亮。这是路由高亮的常见陷阱。

#### `usePathname` vs `useRouter`

| Hook            | 用途                                       |
| --------------- | ------------------------------------------ |
| `usePathname()` | 读取当前路径（响应式，路径变化自动重渲染） |
| `useRouter()`   | 编程式跳转（`router.push('/market')`）     |

两者配合：pathname 用于判断高亮，router 用于点击跳转。

#### 外部链接的两种处理

```typescript
// 桌面端：window.open（新窗口打开外链）
onClick={() => { window.open(item.pathname); handleCloseMore(); }}

// 移动端：Link 组件的 target="_blank"（语义化）
<Link href={item.pathname} target="_blank">{item.name}</Link>
```

两者效果相同，桌面端因为放在 Menu 里需要手动控制，所以用 onClick。

---

### 12.7 `components/SearchInput.tsx` — 通用搜索框

#### 三个回调的设计哲学

```typescript
interface ISearchInput {
  onEnter: (val) => void; // 用户按回车
  onClick: (val) => void; // 用户点击搜索图标
  onClear: () => void; // 用户清空输入
}
```

**为什么不合并成一个 onSearch？**
不同触发方式可能需要不同行为。例如：

- 回车 → 立即搜索 + 上报埋点
- 点击图标 → 立即搜索（不上报，因为已通过输入）
- 清空 → 重置过滤条件 + 隐藏搜索结果区

#### `useRef` vs `useState` 的选择

```typescript
const inputRef = useRef<HTMLInputElement>(); // ① 存输入值
const [isClear, setIsClear] = useState(false); // ② 控制按钮显示
```

- ① 用 useRef：输入值变化不需要触发重渲染（性能更好），只在事件回调里读取
- ② 用 useState：是否显示"清空"按钮需要响应式 UI 更新

**判断依据：是否需要 UI 响应？**

- 需要 → useState
- 不需要（只是存数据） → useRef

#### `:focus-within` 伪类的妙用

```typescript
':focus-within': {
  border: '1px solid #E5FF65',
}
```

`:focus-within` 是"后代元素获得焦点时父元素也响应"的 CSS 伪类。当 InputBase 获取焦点时，外层 Box 的边框变成品牌主色，实现整个容器"被激活"的视觉效果。比传统的 onFocus/onBlur 状态管理简洁很多。

#### 非受控组件写法

```typescript
inputRef.current.value = ''; // 直接操作 DOM
```

清空按钮直接修改 DOM 的 value（**非受控组件**），而不是通过 React state。原因：

- 减少一次 React 重渲染（性能优化）
- 配合 `useRef` 模式自然衔接
- 输入框本来就是 DOM 元素，直接操作语义清晰

注意：非受控组件不适合"需要校验/格式化"的场景，那种情况要用受控组件。

---

### 12.8 第四批跨文件知识总结

#### Service 类的统一模式

```typescript
export default class XxxService {
  private apiClient: ApiClient;
  constructor(apiClient: ApiClient) {  // ← 依赖注入
    this.apiClient = apiClient;
  }
  async getXxx() { return this.apiClient.get(...); }
}
```

**好处：**

1. **共享 HTTP 客户端**：所有 Service 共用同一个 axios 实例（baseURL、拦截器统一）
2. **便于测试**：单测时可以注入 mock 的 ApiClient
3. **职责单一**：Service 只管"业务接口"，不管"HTTP 细节"

#### 容器组件 vs 展示组件

| 类型     | 例子                      | 特点                           |
| -------- | ------------------------- | ------------------------------ |
| 容器组件 | CartDrawer, NavigationAPP | 含状态管理、API 调用、业务逻辑 |
| 展示组件 | SearchInput, CartEmpty    | 纯 UI，props 进、回调出        |

CartDrawer 是典型的容器组件，内部组合多个子组件；SearchInput 是典型的展示组件，可以被任何业务复用。

#### 响应式设计的 3 种方案

| 方案                       | 适用             | 例子                                      |
| -------------------------- | ---------------- | ----------------------------------------- |
| `useMediaQuery` + 条件渲染 | 需要不同 UI 结构 | NavigationAPP（菜单方向不同）             |
| `sx` 断点对象              | 仅样式差异       | `sx={{ width: { xs: '100%', md: 460 } }}` |
| CSS @media                 | 全局样式         | 主题文件中的 breakpoints                  |

---

---

## 十三、第五批文件解读（状态管理 / 搜索 / Swap）

### 13.1 `src/stores/EthscriptionsStore.ts` — 铭文数据缓存 Store

#### 职责

存储当前页面已加载的铭文列表数据，包括三个主要数据集：

| 字段               | 类型           | 用途                           |
| ------------------ | -------------- | ------------------------------ |
| `listedList`       | 挂单列表数据   | 市场页挂单 NFT 列表 + 分页信息 |
| `ownerList`        | 持有者列表数据 | 某集合所有持有者列表           |
| `collectionDetail` | 集合详情       | 当前浏览集合的完整信息         |

#### 乐观更新（Optimistic Update）的妙用

```typescript
export const removeListedItem = (ethscriptionId: string) => {
  if (store.listedList) {
    const _ethsFilter = store.listedList.ethscriptions.filter((item) => item.ethscriptionId !== ethscriptionId);
    store.listedList = { ...store.listedList, ethscriptions: _ethsFilter };
  }
};
```

**为什么叫"乐观更新"？**
用户买成功后，不等后端刷新接口，前端**立即**把购买的那条记录从列表中删除，让用户感觉系统响应很快。如果后续后端返回失败，再回滚数据。这是提升用户体验的常见设计。

**注意：** 这里用 `{ ...store.listedList, ethscriptions: _ethsFilter }` 而不是 `splice`，原因是 listedList 是一个包含 `ethscriptions` 和 `page` 的对象——直接给对象属性赋新值可以同时更新对象的多个层级，用展开语法保留 `page` 字段。

---

### 13.2 `src/stores/FacetSwapStore.ts` — Facet Swap 状态 Store

#### 职责

管理 Facet L2 上 Swap 兑换功能的全局状态，包括：

- 交易对列表（`pairs`）
- 当前选中的 token0 / token1
- 兑换方向（`isExactTokens`：精确输入 vs 精确输出）
- 兑换界面的所有临时状态

#### 核心概念：exactInput vs exactOutput

```
isExactTokens = true  → 精确输入模式：用户指定花多少 A，得到多少 B 由系统计算
isExactTokens = false → 精确输出模式：用户指定要多少 B，花多少 A 由系统计算
```

类比：

- 精确输入："我有 100 USDC，能换多少 ETH？"
- 精确输出："我要 0.05 ETH，需要多少 USDC？"

#### `setPairs` 的智能初始化

```typescript
export const setPairs = (pairs: PairsType[]) => {
  store.pairs = pairs;
  // 同时自动设置 token0/token1 的初始选中状态
  if (pairs.length > 0) {
    store.token0 = { ...store.token0, selected: pairs[0].tokens[0] };
    store.token1 = { ...store.token1, selected: pairs[0].tokens[1] };
  }
};
```

一次调用同时做了三件事：存储 pairs 列表、初始化 token0 选中项、初始化 token1 选中项。避免调用方写三行代码。

#### `clearToken` 的防状态残留设计

当用户离开 Swap 页面时调用，清空所有兑换临时状态。防止下次进入 Swap 页时还显示上次的状态（如残留的数量输入、错误状态等）。

---

### 13.3 `src/components/MutipleTypeSearchInput.tsx` — 多类型搜索框

#### 和普通 `SearchInput` 的核心差异

| 特性       | `SearchInput.tsx`          | `MutipleTypeSearchInput.tsx` |
| ---------- | -------------------------- | ---------------------------- |
| 左侧装饰   | 无                         | 有 Select 类型选择器         |
| 输入框控制 | 非受控（useRef）           | 受控（value={searchBy}）     |
| 使用场景   | Header / 首页              | /search 搜索页               |
| Props      | 3个（onEnter/Click/Clear） | 7个（多了类型和受控相关）    |
| 响应式宽度 | 固定写法                   | `isMobile ? '90vw' : 640`    |

#### 受控组件的关键优势

受控组件的输入值由父组件的 state 控制。这意味着：

- **类型变化时可以同时重置输入内容**：父组件的 `onSearchTypeChage` 回调里可以同时 `setSearchBy('')`
- **父组件总是掌握最新状态**：不需要通过 ref 读取，直接用 state 变量

#### `startAdornment` 嵌套 Select 的布局技巧

```tsx
startAdornment={
  <InputAdornment position="start">
    <Select ...>  {/* 类型下拉嵌在 InputBase 左侧装饰区 */}
      ...
    </Select>
    <Divider orientation="vertical" sx={{ height: '16px' }} />
    {/* 竖线分隔 Select 和输入文字区域 */}
  </InputAdornment>
}
```

将 `Select` 嵌在 `InputAdornment` 里，使其成为输入框的一部分，视觉上是一个整体搜索框，交互上又是独立的下拉控件。

#### 搜索按钮悬停特效

```tsx
'&:hover': {
  background: '#E5FF65',  // 荧光黄绿背景
  svg: {
    fill: '#171A1F',      // 图标变深色（保证对比度）
  },
},
```

hover 时背景变品牌主色，图标变深色，视觉上有"激活"的感觉，引导用户点击。

---

### 13.4 `src/containers/SearchEthsResults/index.tsx` — 搜索结果无限滚动容器

#### 整体数据流

```
URL query params（?searchType=text&searchBy=abc）
     ↓
searchParams = useSearchParams()
     ↓
filterRequest 状态（useImmer）
     ↓
getRecentEths() 触发 API 请求
     ↓
list 状态更新（追加新数据）
     ↓
InfiniteScroll 展示铭文卡片网格
```

#### 两个 useEffect 的职责分离

```typescript
// Effect #1：同步 URL 搜索词到请求参数
useEffect(() => {
  setFilterRequest((state) => {
    state.searchBy = searchBy;
  });
}, [searchBy]); // 依赖：URL 的 searchBy 变化

// Effect #2：请求参数变化时触发 API
useEffect(() => {
  getRecentEths();
}, [filterRequest]); // 依赖：filterRequest 整体变化
```

**为什么要分开？**
单一职责原则。Effect #1 只负责"同步"，Effect #2 只负责"请求"。如果写在一起逻辑会更复杂，也更难调试。

#### InfiniteScroll 的工作原理

```
用户滚动到列表底部
     ↓
InfiniteScroll 检测到（通过 scroll 事件 + getBoundingClientRect）
     ↓
调用 next()（我们传入的：page.index += 1）
     ↓
filterRequest 变化 → Effect #2 触发 → 请求下一页
     ↓
新数据 concat 到 list.ethscriptions
     ↓
InfiniteScroll 的 dataLength 增加 → 继续监听下一次滚动
```

#### 为什么用 Symbol 作为 key？

```typescript
key={Symbol(`${item.ethscriptionId}${index}`).toString()}
```

React 的 key 必须在同一列表中唯一。如果铭文 ID 重复（比如翻页数据有重叠），用 ID 作为 key 会产生冲突警告。

`Symbol()` 每次调用都生成全球唯一值，即使两次调用用同样的参数，结果也不同。因此每次渲染都是新 key，绝对不会重复。

**注意代价**：每次渲染都会重新创建 Symbol，理论上 React 每次都会视为新节点。实际上由于 DOM 结构稳定，影响极小。

#### `searchType === 'all'` 传空给后端

```typescript
const params = {
  ...filterRequest,
  searchType: searchType === 'all' ? '' : searchType,
};
```

前端用 `'all'` 作为"不限类型"的标识符（UI 友好），但后端约定空字符串表示不限类型。这里做了一次语义转换，防止后端因为收到 `'all'` 字符串而产生错误。

---

### 13.5 `src/app/search/page.tsx` — 搜索页入口

这是一个典型的**薄页面**文件（约10行代码）：

```tsx
const Search = () => (
  <Box>
    <SearchEthsResults />
  </Box>
);
```

**薄页面/厚容器模式的价值：**

- Next.js App Router 要求每个路由必须有 `page.tsx`
- 但具体业务逻辑放在 `containers/SearchEthsResults/` 里
- 这样未来如果需要在其他地方嵌入搜索结果，直接用 `<SearchEthsResults />` 即可

---

### 13.6 `src/app/swap/page.tsx` — 注意：文件名是 swap，实际是 Bridge！

**历史遗留问题说明：**

这个文件虽然路径是 `/swap`，但实际渲染的是 Bridge（跨链桥）功能：

- `LineChat` 组件：Token 价格走势图
- `Deposit` 组件：存入/提出资产表单

从 `NavigationAPP.tsx` 可以看到 Swap 菜单已被注释掉（暂时封印），因此 `/swap` 路径被"借用"来承载 Bridge 功能。这是历史遗留，不影响正常使用。

---

### 13.7 `src/containers/Swap/index.tsx` — Swap 业务容器

#### 三子组件分工

```
Swap（容器）
├── SwapChart（大屏才显示）
│   └── 价格走势图：使用 tokenAddress 查询历史价格数据
├── SwapContainer（始终显示）
│   └── 核心兑换交互：输入代币数量、选代币、执行兑换
└── HirstoryList（始终显示，注意拼写：Hirstory≠History）
    └── 当前用户的兑换历史记录
```

#### 1400px 断点的设计逻辑

```typescript
const matches = useMediaQuery('(min-width:1400px)');

// 大屏（≥1400px）：左图右表单（图+间隔+表单需要足够宽）
// 小屏（<1400px）：只显示表单（隐藏图表，节省空间）
```

Swap 表单本身约 400px 宽，价格图约 600px，加上间距需要 1100px+。设置 1400px 断点留有余量，防止在较窄的笔记本屏幕上布局溢出。

#### tokenAddress 的来源

```typescript
const collectionDetail = ethscriptionsStore.collectionDetail; // 从 Store 读取
const tokenAddress = collectionDetail?.collections.facetStat.contractAddress;
```

`tokenAddress` 是当前集合在 Facet L2 上部署的 ERC-20 合约地址，用于：

- `SwapChart`：查询该 Token 的历史价格
- `HirstoryList`：筛选与该 Token 相关的兑换历史

---

### 13.8 第五批关键知识点总结

#### valtio 与 useImmer 的使用场景对比

| 特性       | valtio (store)             | useImmer (组件内)                |
| ---------- | -------------------------- | -------------------------------- |
| 作用域     | 全局，跨组件共享           | 局部，仅当前组件                 |
| 典型场景   | 购物车、登录状态、集合详情 | 过滤条件、列表数据、表单状态     |
| 持久化     | 可配合 subscribe 持久化    | 组件卸载后状态消失               |
| 响应式原理 | Proxy 代理                 | immer produce 生成新对象         |
| 修改语法   | 直接赋值 `store.x = y`     | `setX(state => { state.x = y })` |

#### InfiniteScroll vs Pagination 的选择

| 特性       | InfiniteScroll（本项目搜索） | Pagination（本项目市场） |
| ---------- | ---------------------------- | ------------------------ |
| 用户体验   | 流畅，无需点击               | 需要点击下一页           |
| 适合内容   | 流式内容（图片流、铭文流）   | 表格数据（需要定位某页） |
| URL 可分享 | 难（没有页码参数）           | 易（`?page=3` 可分享）   |
| SEO        | 较差                         | 较好                     |

---

## 十四、第六批：Swap 深度解析 + Bridge 表单（5个文件）

### 14.1 containers/Swap/SwapContainer/index.tsx — 核心兑换交互区

**职责**：用户输入兑换数量 → 预估输出 → 执行兑换（或先授权）的完整交互 UI。

#### 核心状态

| 变量               | 含义                                                                          |
| ------------------ | ----------------------------------------------------------------------------- |
| `isApprove`        | `BigNumber(token0.amount).gt(token0.allowances)`，花费量超出授权额度时为 true |
| `isSwapLoading`    | 兑换交易等待状态                                                              |
| `isApproveLoading` | 授权交易等待状态                                                              |
| `facetSwapStore`   | valtio 快照（token0/token1/pairs/isExactTokens 等）                           |

#### getSimulate()：Facet 链模拟（dry-run）

```
用户输入 → getSimulate() → services.facet.getSimulate(tx_payload)
                         → 返回 return_value[0]（amountIn）+ [1]（amountOut）
                         → 更新 token0/token1 的 amount 显示
```

- `isExactTokens=true`（精确输入）→ 用 `swapExactTokensForTokens` payload
- `isExactTokens=false`（精确输出）→ 用 `swapTokensForExactTokens` payload
- 模拟失败（如流动性不足）→ 清空 token0.amount

#### useEffect 触发时机

```
facetSwapStore.token0?.amount 变化
  → BigNumber(amount).gt(0) 且 !isApprove
  → 调用 getSimulate()（防抖 500ms 已在输入框的 onValueChange 中设置）
```

#### handleTransfer() / handleIncreaseAllowance()

两者都调用 `evmService.etchMarket.transferEthscriptionFacet()`，区别在于 payload：

- `handleTransfer`：`swapExactTokensForTokens` 或 `swapTokensForExactTokens`
- `handleIncreaseAllowance`：`approveFacet({ args: [routerAddress, 2^255] })`（几乎无限授权）

#### UI 结构

```
SwapContainer
├── 标题 "Swap" + "Powered by FacetSwap.com" 链接
├── token0 输入框（NumericFormat + SelectTokenFacet + Max 按钮）
├── SwitchButton（切换 token0/token1 方向）
├── token1 输出框（disabled，只读，模拟结果填充）
└── WalletConnectButton
    ├── isApprove=true  → LoadingButton "Increase Allowance"
    └── isApprove=false → LoadingButton "Swap"
```

---

### 14.2 containers/Swap/SwapChart.tsx — Token 价格走势图

**职责**：展示当前 Token 的价格折线图 + 市场统计（流动性、成交量等），仅宽屏（≥1400px）显示。

#### 数据流

```
useQuery('getMarketchartSwap', filterRequest)
  ↓ enabled: collectionName != ''
  → services.marketplace.marketChart(filterRequest)
  → lineData（prices/currentPrice/priceChangePercentage24h/liquidity 等）
```

#### 关键逻辑

| 要点                                                        | 说明                                                                    |
| ----------------------------------------------------------- | ----------------------------------------------------------------------- |
| `numberFormatUnit(value)`                                   | 大数转带单位格式：`1500000 → { value: 1.5, unit: 'M' }`                 |
| `percentage24hColor`                                        | `priceChangePercentage24h <= 0` → 红色 `#D8346F`，否则 → 绿色 `#32CA8A` |
| `LineStats.onSelect(val)`                                   | 切换 period（1d/1w/1m/1y）→ 更新 filterRequest → useQuery 自动重新请求  |
| `prices.map(a => ({ ...a, usdPrice: Number(a.usdPrice) }))` | string → number，LineChart 要求 number 类型                             |

---

### 14.3 containers/Swap/HirstoryList/index.tsx — 兑换历史列表

**职责**：展示当前 Token 的 Facet L2 兑换历史记录（带分页）。

> 注意：`HirstoryList` 是原代码的拼写错误，应为 `HistoryList`。

#### 三个 useEffect 的分工

```
Effect #1：监听 facetSwapStore.historyAddress（用户切换代币时）
  → setFilterRequest({ tokenAddress: historyAddress, 'page.index': 1 })

Effect #2：监听 tokenAddress prop（父组件传入初始地址时）
  → setFilterRequest({ tokenAddress, 'page.index': 1 })

Effect #3：监听 filterRequest 变化（包括翻页、切换代币）
  → getTransactionList()
```

#### 为什么有两个地址来源？

- `tokenAddress`（prop）：页面首次加载时由父组件传入
- `facetSwapStore.historyAddress`（store）：用户在 SwapContainer 切换代币时实时更新

两者都要响应，所以需要两个独立的 effect。

#### 防并发机制

```ts
async function getTransactionList() {
  if (isLoading) return;  // 上一次请求未完成，直接返回，不重复发请求
  ...
}
```

---

### 14.4 containers/Bridge/Deposit.tsx — 跨链存入表单（开发中）

**职责**：提供 ETH → Facet L2 的跨链存入操作表单。

> ⚠️ **当前状态**：功能未完成。`onSubmit` 函数体内只有 `// 提交` 注释，实际的跨链逻辑尚未实现。

#### react-hook-form 用法详解

本文件是项目中使用 react-hook-form 最完整的示例：

```
useForm<IFormValues>()
  ├── handleSubmit(onSubmit) → 包裹 form 的 onSubmit，自动触发验证
  ├── control → 传给 Controller，注册受控字段
  ├── formState.errors → 传给 ErrorMessage 显示验证错误
  ├── formState.isSubmitting → 控制 LoadingButton 的 loading 状态
  ├── formState.isValid → 控制提交按钮的 disabled 状态
  ├── reset() → 提交成功后清空表单
  └── trigger('amount') → 每次输入时手动触发验证（实时反馈）
```

#### Controller + NumericFormat 组合模式

```tsx
<Controller
  name="amount"
  control={control}
  rules={{ required: true, max: { value: 100, message: 'Invalid balance' } }}
  render={({ field }) => {
    const { onChange, ...fieldProps } = field;
    return (
      <NumericFormat
        {...fieldProps} // 注入 value/name/ref/onBlur
        onValueChange={(data) => {
          onChange({ target: { name: field.name, value: data.value } });
          trigger('amount'); // 手动触发实时验证
        }}
      />
    );
  }}
/>
```

**为什么不用 register()**：`NumericFormat` 是第三方组件，不接受 `ref` 注入，所以必须用 `Controller` 的 render prop 模式。

---

### 14.5 containers/Bridge/LineChat.tsx — Bridge 价格图表（⚠️ 假数据）

> ⚠️ **重要警告**：此组件使用的全部是硬编码假数据，Bridge 功能尚未完成！

#### 假数据清单

| 位置                            | 假数据内容                 | 应替换为                  |
| ------------------------------- | -------------------------- | ------------------------- |
| `lineData`                      | 写死 4 个点的数组          | 从 API 获取的真实价格序列 |
| `$12,456.44`                    | 写死的价格字符串           | 实时 ETH 价格             |
| `-7.33`                         | 写死的涨跌值               | 真实 24h 涨跌             |
| `false ? '#32CA8A' : '#D8346F'` | 条件永远为 false，始终红色 | 真实涨跌判断              |
| `InvestInfo data`               | TVL/Volume 等写死字符串    | API 返回的真实数据        |
| `LineStats.onSelect`            | 请求函数全被注释掉         | 接入真实数据请求          |

#### 为什么保留假数据？

这是常见的前端开发模式：先做 UI 骨架（样式/布局），后接数据。
开发者用假数据验证 UI 效果，接入真实 API 时再替换。

---

### 14.6 第六批关键知识点总结

#### react-hook-form 三种注册方式对比

| 方式               | 适用场景                                   | 原理                                |
| ------------------ | ------------------------------------------ | ----------------------------------- |
| `register('name')` | 原生 input / MUI TextField                 | 注入 ref，直接操控 DOM              |
| `Controller`       | 第三方受控组件（NumericFormat、Select 等） | render prop 手动桥接 onChange/value |
| `useController()`  | 需要在自定义组件内部使用 hook 时           | Controller 的 hook 等价形式         |

#### Facet L2 三步交互模式

```
第 1 步：getSimulate(tx_payload)
  → Facet 链 dry-run，不花 Gas，预估输出量

第 2 步（若 isApprove=true）：handleIncreaseAllowance()
  → transferEthscriptionFacet({ payload: approveFacet(...) })
  → 向 routerAddress 授权"几乎无限"额度

第 3 步：handleTransfer()
  → transferEthscriptionFacet({ payload: swapExactTokensForTokens(...) })
  → 真实兑换，广播到以太坊主网，Facet 节点监听并执行
```

#### "假数据开发模式"的识别特征

当你看到以下代码时，说明该功能尚未完成：

1. 写死的数字/字符串（如 `$12,456.44`、`$802.2M`）
2. 条件判断永远为 `true` 或 `false`（如 `false ? A : B`）
3. 有意义的函数调用被注释掉（如 `// getCoinInfo(coinDays)`）
4. 函数体只有 `// 提交` 注释

---

---

## 十五、第七批：质押模块（Staking）全解析（8个文件）

### 15.1 app/staking/layout.tsx — 质押页布局

质押模块的布局文件，与其他模块（market/swap）结构完全一致：

- 顶部 `paddingTop: 64px`（为固定导航栏留空间）
- 内容区 `minHeight: calc(100vh - 203.5px)`（占满剩余视口）
- 内容宽度最大 1160px，水平居中
- 底部插入 `<Footer />`

---

### 15.2 app/staking/page.tsx — 质押列表页（薄页面）

**职责**：读取 URL 参数 `?collectionName=xxx`，传给子容器，自身只负责布局。

```
URL: /staking?collectionName=FacetPunks
         ↓ useSearchParams()
     collectionName = "FacetPunks"
         ↓
     ┌────────────────┐
     │ CollectionListSimple │  ← 集合选择下拉
     ├────────────────┤
     │ StakingOverview      │  ← 概览统计 + Era 进度
     ├────────────────┤
     │ StakingRankList      │  ← 质押排行榜
     └────────────────┘
```

---

### 15.3 app/staking/DetailBack.tsx — 返回按钮

一个通用的返回箭头组件，接受 `label` prop（如 `"Staking / 0x123..."`），
点击调用 `router.back()` 返回上一个路由历史。

---

### 15.4 app/staking/detail/page.tsx — 质押详情页（薄页面）

**职责**：读取 URL 参数 `?owner=0x...&collectionName=xxx`，渲染某地址的质押记录。

```
URL: /staking/detail?owner=0xabc&collectionName=FacetPunks
         ↓ useSearchParams()
     owner = "0xabc", collection = "FacetPunks"
         ↓
     DetailBack（← Staking / 0xabc）
     StakingDetailList（owner + collection）
```

---

### 15.5 containers/StakingOverview.tsx — 质押概览面板

**职责**：展示 6 个质押统计指标 + Era 圆形进度条。

#### getErc20Info() 的数据处理

```ts
// 1. 请求质押统计数据
const response = await services.vault.getStakingStatic(collection);

// 2. 计算 Era 总时长（天）
const diff = dayjs(epochEndTime * 1000).diff(epochStartTime * 1000, 'day');

// 3. 计算 Era 完成进度（百分比）
const progress = (BigNumber(now - epochStartTime) / BigNumber(epochEndTime - epochStartTime)) * 100;

// 4. 计算剩余时间字符串（"XM XD left"）
const left = getTimeAgoString(epochEndTime);
```

#### getTagComponent() 内联函数模式

为什么用函数而不是抽出独立组件？
因为 6 个统计格子只在 `StakingOverview` 内使用，抽组件会增加文件数量，
用内联函数保持简洁，Props 只有 5 个，复杂度可控。

#### 6 个统计指标

| 指标               | 说明         | 单位      | 高亮      |
| ------------------ | ------------ | --------- | --------- |
| Stakers            | 质押人数     | 人        | ✅ 荧光黄 |
| Total locked       | 锁仓总量     | 代币      | ❌        |
| TVL                | 总锁仓价值   | ETH       | ❌        |
| Cumulative rewards | 累计奖励     | ETH       | ❌        |
| Pending rewards    | 待领取奖励   | ETH       | ❌        |
| Era                | 当前周期进度 | 天/百分比 | ❌        |

---

### 15.6 containers/StakingDetailList/index.tsx — 某地址质押记录列表

**职责**：展示指定 owner 在指定集合中的所有质押记录，支持分页。

#### 与 StakingRankList 的区别

| 对比项   | StakingDetailList                     | StakingRankList                 |
| -------- | ------------------------------------- | ------------------------------- |
| 数据来源 | `getStakingRecord(owner, collection)` | `getStakingRank(collection)`    |
| 请求参数 | 包含 owner（个人记录）                | 无 owner（全集合排名）          |
| 空值保护 | 无（父组件保证 owner 非空）           | 有（`collection.trim() != ''`） |
| 列表数组 | `records`                             | `stakes`                        |

#### 防抖 + 防并发的双重保护

```ts
filterRequest → useDebounce(300ms) → filterRequestDebounce
                                           ↓ useEffect 监听
                                     if (!isLoading) → 发请求
```

---

### 15.7 containers/StakingRankList/index.tsx — 质押排行榜

**职责**：展示集合内所有质押者按质押量排序的排行榜，支持分页，点击行跳转到详情页。

#### 两个 useEffect 的分工

```
Effect #1：防抖后的 filterRequest 变化
  → 判断 collection 非空
  → getCollectionList()

Effect #2：collection prop 变化
  → 更新 filterRequest（collection + page.index 重置为 1）
  → 触发 Effect #1
```

#### 为什么 StakingRankList 要判断 collection 非空，StakingDetailList 不用？

`StakingRankList` 在主页（/staking）渲染，
页面加载时 collectionName 可能还是空字符串（用户还没选集合），
所以需要 `collection.trim() != ''` 保护。

`StakingDetailList` 在详情页（/staking/detail）渲染，
URL 中必定携带 owner 和 collectionName，父组件已保证非空，无需额外保护。

---

### 15.8 第七批关键知识点总结

#### "Era"（时代/周期）的业务含义

```
时间轴：
epochStartTime ──────────── now ──────── epochEndTime
     │                       │                │
   开始                   当前时间           结束
     └──── 已过 X% ────────┘└── 还剩 Y 天 ──┘
```

质押系统周期性（Era）分配奖励，每个 Era 结束后结算并开始新一轮。

#### useDebounce vs debounce 的使用场景

| 场景                           | 用哪个                                  | 原因                  |
| ------------------------------ | --------------------------------------- | --------------------- |
| 输入框（onChange）             | `debounce(fn, ms)`（lodash）            | 直接包裹事件处理函数  |
| 请求参数对象（useImmer state） | `useDebounce(value, ms)`（usehooks-ts） | Hook 用法，监听值变化 |

`useDebounce` 返回的是延迟后的"快照值"，useEffect 依赖它，
这样每次 filterRequest 变化后等 300ms，如果期间没有新变化才触发请求。

#### 内联函数 vs 独立组件的权衡

```
getTagComponent() 用内联函数的理由：
✅ 只在 StakingOverview 内部使用（无复用需求）
✅ Props 简单（5 个原始类型）
✅ 避免创建额外文件
❌ 如果需要在其他页面复用，就应该提取为独立组件
```

---

## 十六、第八批内容预告

第七批已完成（代码注释）：

- ✅ `app/staking/layout.tsx` 质押页布局
- ✅ `app/staking/page.tsx` 质押列表页
- ✅ `app/staking/DetailBack.tsx` 返回按钮
- ✅ `app/staking/detail/page.tsx` 质押详情页
- ✅ `containers/StakingOverview.tsx` 质押概览面板
- ✅ `containers/StakingDetailList/index.tsx` 质押记录列表
- ✅ `containers/StakingRankList/index.tsx` 质押排行榜

第八批将包含：

- 📋 `services/vault/` — 质押相关 API 服务层
- 📋 `containers/VaultList/` — 金库列表
- 📋 `app/tokens/` — Token 页面模块
- 📋 `containers/TokenOverview.tsx` — Token 概览
- 📋 `containers/TokenList/` — Token 列表
- 📋 深度专题：DataGrid 的列定义与自定义渲染器

> 📌 **说"继续"即可开始第八批内容**

---

_文档版本：第七批（2026年6月）_  
_项目：EtchMarket — 以太坊铭文交易平台_

---

## 十六、第八批：金库模块 & Token 铭文模块（8个文件）

本批次覆盖：金库（Vault）赎回流程、质押（Staking）API、Token 铭文列表/详情页。

---

### 16.1 `services/vault/index.ts` — 金库 & 质押 API 服务层

**VaultService 类**，封装所有金库和质押相关的 HTTP 接口。

| 方法                   | HTTP | 路径                                 | 说明                                                |
| ---------------------- | ---- | ------------------------------------ | --------------------------------------------------- |
| `getVaultList`         | GET  | `/vaults/ethscriptions?...`          | 用户在金库中的铭文列表                              |
| `getVaultBalance`      | GET  | `/vaults/balance/{address}?...`      | 金库余额（available/transferable/stakingLockRatio） |
| `getVaultEthscription` | GET  | `/vaults/ethscriptions/split?...`    | 赎回前的铭文分批数据                                |
| `getStakingStatic`     | GET  | `/staking/statistics/{collection}`   | 质押统计（stakers/TVL/奖励）                        |
| `getStakingRank`       | GET  | `/staking/ranking/{collection}?...`  | 质押排行榜                                          |
| `getStakingRecord`     | GET  | `/staking/records/{owner}?...`       | 个人质押记录                                        |
| `postVaultRedeem`      | POST | `/vaults/ethscriptions/redeem`       | 发起赎回（提交签名）                                |
| `getVaultWithdraw`     | GET  | `/vaults/ethscriptions/withdraw?...` | **轮询**获取提取签名                                |

#### ★ 核心亮点：轮询机制（`getVaultWithdraw`）

铭文提取（Withdraw）是**异步两步操作**：

```
用户点击"提取" → postVaultRedeem（提交签名）
     ↓
后端异步生成可信签名（trustedSignature）
     ↓
前端轮询 getVaultWithdraw，每 2 秒请求一次
     ↓
trustedSignature 非空 → 停止轮询
     ↓
用前端用 trustedSignature 调用链上合约完成提取
```

```typescript
while (isRequest) {
  const response = await this.apiClient.get(`/vaults/ethscriptions/withdraw?${queryParams}`);
  if (response?.code == 200) {
    if (response.data.trustedSignature.trim() != '') {
      isRequest = false;     // 签名已生成，停止循环
      return response.data;
    } else {
      await delay(2000);     // 等 2 秒再试
    }
  } else {
    throw new Error(...);    // 接口报错，终止轮询
  }
}
```

**为何用轮询而不用 WebSocket？**  
Web3 项目中，签名生成是低频操作（每次赎回才触发一次），不值得维护长连接。  
轮询简单可靠，2 秒一次完全够用，且不需要处理 WebSocket 的连接/断线/重连问题。

---

### 16.2 `services/vault/types.ts` — 类型定义

关键类型解析：

**`PostVaultRedeemRequest`（赎回请求）**：

- `collection`、`ethscriptionId`：指定哪条铭文
- `orderHash`：订单哈希，用于生成签名
- `signer`：签名者地址
- `signature`：用户用私钥对 orderHash 的签名

**`GetVaultWidthdrawData`（提取数据）**：

- `signature`：用户签名
- `trustedSignature`：后端生成的可信签名（这个才是链上提取的凭证）

**`GetStakingStaticData`（质押统计）**：

- `stakers`：参与质押的人数
- `tvl`：总锁仓价值（ETH 计价）
- `epochStartTime/epochEndTime`：当前 Era 的时间范围（质押奖励是按 Era 分配的）
- `pendingRewards`：全局待领取奖励

---

### 16.3 `containers/VaultList/index.tsx` — 金库铭文列表

**三个 useEffect 的分工**：

```
Effect #1: [filterRequest] → getMarketList()   — 核心请求
Effect #2: [collectionName] → 更新 filterRequest.collection
Effect #3: [address] → 更新 filterRequest.owner
```

这是"参数变化 → 更新 filterRequest → 触发 Effect #1 请求"的**间接触发模式**。  
Effect #2/#3 自身不请求数据，只修改参数，由 Effect #1 统一处理请求。

**`handleOnChangeEthscription`：本地状态更新（避免重新拉取整个列表）**

当用户操作某条铭文（如点击赎回）时，不重新请求整个列表，而是在本地直接修改对应 item：

```typescript
// op='update'：铭文状态变更（如"赎回中"）
const _ethscriptions = list.ethscriptions.map((item) =>
  item.order.ethscriptionId === target.order.ethscriptionId ? newItem : item,
);

// op='remove'：铭文赎回完成，从列表移除
const _ethscriptions = list.ethscriptions.filter((item) => item.order.ethscriptionId !== target.order.ethscriptionId);
```

优点：减少不必要的网络请求，操作响应更快。

---

### 16.4 `app/tokens/layout.tsx` — 布局容器

**为什么 metadata 被注释掉了？**

```typescript
// 这段代码是错误的，不能工作：
'use client'; // ← 这行声明了"客户端组件"
export const metadata = { title: '...' }; // ← Next.js 禁止在客户端组件中导出 metadata
```

Next.js 的 `metadata` API 是**服务端专属**功能，只能在服务端组件（Server Component）中使用。  
一旦加了 `'use client'`，整个文件就变成了客户端组件，`metadata` 导出会被 Next.js 忽略或报错。  
解决方案：要么去掉 `'use client'`，要么把 metadata 移到单独的服务端文件。

---

### 16.5 `app/tokens/page.tsx` — Token 铭文主页

**"薄页面"模式**：页面本身只做布局和导航，实际数据展示交给 `TokenList` 容器。

`SearchInput` 三个回调的导航逻辑：

```typescript
onClear  → router.replace('/tokens/search')          // 清空 → 无参搜索页
onClick  → router.replace('/tokens/search?address=xxx') // 有参搜索
onEnter  → router.replace('/tokens/search?address=xxx') // 回车同上
```

使用 `replace` 而非 `push` 的原因：不产生历史记录，用户按返回键不会回到搜索中间态。

---

### 16.6 `app/tokens/ResultArea.tsx` — 铭文查询结果展示区

**Props 设计**：纯展示组件，不发请求，只接收数据和回调。

```typescript
interface IResultArea {
  data: GetOwnerData; // 铭文数据（已由父组件请求好）
  loadMore: () => void; // 加载更多（由父组件实现）
}
```

**卡片点击 → 外部链接**：

```typescript
onClick={() => {
  const url = `${URL_CONFIG[chainId].etherscription}/ethscriptions/${item.ethscriptionId}`;
  window.open(url); // 新标签打开官方铭文浏览器
}}
```

---

### 16.7 `containers/TokenList/index.tsx` — ERC-20 列表

**useRef 读取 DOM vs useState 的权衡**：

| 方案                       | 特点                         | 适用场景                   |
| -------------------------- | ---------------------------- | -------------------------- |
| `useState + onChange`      | 每次按键都触发 re-render     | 需要实时验证/展示输入内容  |
| `useRef + onKeyUp/onClick` | 只在提交时读取值，无额外渲染 | 只需提交时获取值（搜索框） |

本组件使用 `useRef`，只在用户按 Enter 或点击搜索按钮时读取 `inputRef.current?.value`，  
有效减少了搜索过程中的无谓重渲染。

**特殊情况**：用户清空输入框时 `onInput` 立即重置 `tokenQuery`，恢复全量列表（不需要按回车）。

---

### 16.8 `containers/TokenOverview.tsx` — Token 铸造详情

**isErc20S 两路铸造逻辑**：

```
isErc20S = (p === 'erc--20') && ca 非空

isErc20S=true  → erc20sEthscribe()
  → evmService.erc20Ethscriptions.ethscribe({ singer, address: ca })
  → 直接调链上合约，合约内部检查 blockInterval、walletLimit 等规则

isErc20S=false → handleGetEthscribe()
  → services.ethscriptions.getEthscribe({ p, tick, nonce })
  → evmService.etchMarket.inscribeEthscription({ singer, data })
  → 先从后端获取铭文数据，再发 Ethscription 交易
```

**blockInterval 冷却机制**（erc-20s 专属）：

```typescript
const lastBlock = await evmService.erc20Ethscriptions.getlLastInscribeBlock({
  contractAddress,
  userAddress,
});
const cur = await provider.getBlockNumber();
// 如果距离上次 mint 的区块间隔 <= blockInterval，则仍在冷却期
setIsWaiting(cur - lastBlock <= Number(erc20Info.erc20s.blockInterval));
```

按钮 UI 根据状态切换：

- `isWaiting=true` → 显示 `'Waiting...'` + 禁用
- 全部铸造完（minted >= totalSupply）→ 禁用
- 用户达到 walletLimit → 禁用

---

### 16.9 第八批关键知识点总结

**知识点一：轮询 vs WebSocket 的选择**

| 对比项     | 轮询（Polling）     | WebSocket              |
| ---------- | ------------------- | ---------------------- |
| 实现难度   | 低（while + delay） | 高（需要管理连接状态） |
| 实时性     | 较低（2秒延迟）     | 高（毫秒级）           |
| 服务器负载 | 较高（频繁请求）    | 低（长连接推送）       |
| 适用场景   | 低频异步操作        | 高频实时消息           |

结论：**赎回签名是低频一次性操作**，用轮询完全够用，不值得引入 WebSocket 的复杂性。

**知识点二：useRef 读取 DOM 的正确使用场景**

- ✅ 搜索框提交（只需提交时获取值）
- ✅ 第三方库 DOM 操作（如图表库）
- ✅ 管理定时器/订阅（存储不影响渲染的值）
- ❌ 实时验证（用 useState，因为需要渲染）
- ❌ 受控表单（用 useState，让 React 管理表单状态）

**知识点三：Next.js 客户端组件的 metadata 限制**

- `'use client'` 文件 → 禁止 `export const metadata`
- 需要 metadata 时：去掉 `'use client'` 或把 metadata 移到 `layout.tsx`（不加 `'use client'`）

---

### 第八批文件清单

第八批已完成（代码注释）：

- ✅ `services/vault/index.ts`
- ✅ `services/vault/types.ts`
- ✅ `containers/VaultList/index.tsx`
- ✅ `app/tokens/layout.tsx`
- ✅ `app/tokens/page.tsx`
- ✅ `app/tokens/ResultArea.tsx`
- ✅ `containers/TokenList/index.tsx`
- ✅ `containers/TokenOverview.tsx`

---

## 十七、Token 详情页、地址查询页与铭文卡片通用容器（第九批）

### 17.1 app/tokens/info/page.tsx — Token 铭文详情页

**路由**：`/tokens/info?p=erc-20&tick=ordi`

**职责**：薄页面（布局+参数读取），负责展示某个具体 Token 的详情，由三部分组成：

| 区块     | 组件                               | 说明                                     |
| -------- | ---------------------------------- | ---------------------------------------- |
| 顶部导航 | `IconButton` + `EthscriptionLabel` | 返回按钮 + 集合标签（显示 `{p} {tick}`） |
| 铸造信息 | `TokenOverview`                    | 进度条、数据面板、Ethscribe 按钮         |
| 列表表格 | `TokenTables`                      | Holders / Transfers Tab 切换             |

**参数读取**：通过 `useSearchParams` 读取 URL query string：

```typescript
const data = { p: queryParams?.get('p') ?? '', tick: queryParams?.get('tick') ?? '' };
```

**EthscriptionLabel 用法**：

- `collectionName` 传入 `"erc-20 ordi"`（协议名+空格+tick）
- 这是集合标签组件的典型用法：显示协议归属

---

### 17.2 app/tokens/search/page.tsx — Token 地址查询页

**路由**：`/tokens/search?address=0x...&collection=...&category=token`

**职责**：通过钱包地址查询该地址持有的所有铭文（Token 类型），带集合过滤和无限滚动。

**URL 参数**：

- `address` = 钱包地址（必须，否则不发请求）
- `collection` = 集合名过滤（可选）
- `category` = 类别过滤（可选，目前只启用 token）

**数据流**：

```
URL address/collection 变化
  → useEffect([address, collection])
    → getOwnerList(1)
      → pageIndex=1: setOwnerList(() => response.data)（整体替换）
      → pageIndex>1: concat 追加（无限滚动）
```

**空结果处理**：

```typescript
// 使用 toast.custom 显示完全自定义的样式（不是普通 toast）
toast.custom(
  <Box sx={{ width: '380px', ... }}>
    <WarnSVG style={{ marginRight: '12px' }} />
    <Typography>No Result Found!</Typography>
  </Box>,
);
```

**onEnter 双重操作**（快速响应技巧）：

```typescript
onEnter={(value) => {
  getOwnerList(1);              // 立即请求（不等 URL 变化触发 effect）
  router.replace(`/tokens/search?address=${value}`); // 再同步 URL
}}
```

**集合过滤器**：`EthscriptionCollectionOwner` 组件  
选中集合 → URL 写入 collection 参数 → useEffect 重新请求 → 列表更新  
目前只有 `category="token"` 启用，domain/nft 版本被注释掉。

---

### 17.3 app/tokens/search/ResultArea.tsx — 搜索结果展示区

**与 app/tokens/ResultArea.tsx 的区别**：

| 对比项     | tokens/ResultArea     | tokens/search/ResultArea       |
| ---------- | --------------------- | ------------------------------ |
| 网格列宽   | `306px`（固定）       | `minmax(256px, 1fr)`（自适应） |
| 左右内边距 | `p: '28px 12% 0 12%'` | 无（全宽布局）                 |
| 适用场景   | Token 列表总览页      | 地址搜索结果页                 |

自适应网格说明：

```css
gridtemplatecolumns: 'repeat(auto-fill, minmax(256px, 1fr))';
/* 每列至少 256px，多余空间平均分配，自动换行 */
```

---

### 17.4 containers/TokenTables.tsx — Token 详情表格切换容器

**职责**：在 Token 详情页展示两个 Tab 切换的数据表格。

**枚举切换模式**：

```typescript
enum TokenTab {
  Holders = 'Holders', // 持有者列表
  Transfers = 'Transfers', // 转账记录
}
const [currentTab, setCurrentTab] = useState<TokenTab>(TokenTab.Holders);
```

**为什么用枚举而不是字符串**：枚举值由编译器检查，不会出现拼写错误（TypeScript 优势）。

**条件渲染**：

```tsx
{
  currentTab === TokenTab.Holders ? (
    <HoledersList p={p} tick={tick} ca={ca} />
  ) : (
    <TransferList p={p} tick={tick} ca={ca} />
  );
}
```

Props（p/tick/ca）全部透传给子容器，子容器自己负责请求数据。

---

### 17.5 containers/Erc20TokenList/index.tsx — ERC-20s 铭文列表容器

**与 containers/TokenList/index.tsx 的核心区别**：

| 对比项   | TokenList      | Erc20TokenList      |
| -------- | -------------- | ------------------- |
| API 接口 | `getErc20`     | `getErc20sList`     |
| 数据类型 | `GetErc20Data` | `GetErc20sListData` |
| 部署按钮 | `DeployERC20`  | `DeployERC20s`      |
| 默认状态 | `'InProgress'` | `'All'`             |
| 容器宽度 | 固定 `1160px`  | 全宽 `xs: '100%'`   |

**状态过滤 Tab**：All / New / In-progress / Completed  
**搜索框逻辑**：回车/点击搜索图标 → 更新 `tokenQuery` + 重置 `page.index = 1`  
**响应式**：`isMobile`（宽 < 750px）控制搜索框和按钮的布局位置

---

### 17.6 containers/EthscriptionBox/index.tsx — 铭文卡片通用容器

这是整个项目中最被复用的组件之一，市场列表、我的资产、金库等所有铭文卡片都通过此容器渲染。

**addressDisplay 三层判断逻辑**：

```typescript
// 第一层（默认）：截断地址
let addressDisplay = `${owner.slice(0, 5)}...${owner.slice(-4)}`;

// 第二层：是自己的地址 → 显示 'you'
if (address && getAddress(address) === getAddress(owner)) {
  addressDisplay = 'you';
}

// 第三层：owner 是协议地址 → 说明铭文在市场挂单中 → 显示 'market'
if (protocolAddress.trim() != '' && getAddress(owner) === getAddress(protocolAddress)) {
  addressDisplay = 'market';
}
```

注：`getAddress`（ethers.js）做校验和规范化，防止大小写不一致导致比较失败。

**高亮边框机制**：

```typescript
const isInCart = cartStore.orderIds?.find((id) => id === ethscription.order.orderId);
const isInEthlist =
  bulkListingStore.ethsList.findIndex((item) => item.order.ethscriptionId === ethscription.order.ethscriptionId) !== -1;
const isHighlight = Boolean(isInCart) || isInEthlist;

// 边框颜色：高亮时 #D5E970，默认 #2F343E
border: `1px solid ${isHighlight ? '#D5E970' : '#2F343E'}`;
```

**Context Provider 数据下传**：

```tsx
<EthscriptionBoxContext.Provider value={{ ethscription, onChange, isInCart: Boolean(isInCart) }}>
  {/* 所有子组件（含 footer 插槽）都能通过 useEthscriptionBoxContext() 读取 */}
</EthscriptionBoxContext.Provider>
```

**批量选择 Checkbox（isMyEtchScription=true 时）**：

- 默认隐藏（`display: 'none'`）
- 悬停时出现（`'&:hover': { '& .checkbox': { display: 'flex' } }`）
- 批量列表非空时一直显示（`bulkListingStore.ethsList.length > 0`）
- 点击阻止冒泡，再调用 `BulkListingStore.toggleEthscriptionToCart`

**footer 插槽**：允许父组件插入任意操作按钮（买入/质押/赎回/挂单等），EthscriptionBox 不关心具体操作逻辑，实现**关注点分离**。

---

### 17.7 containers/EthscriptionBox/EthscriptionBoxContext.tsx — 铭文卡片 Context

**为什么用 React Context？**

EthscriptionBox 允许父组件传入任意 `footer` 插槽（`JSX.Element`），footer 里的操作按钮需要访问铭文数据，但如果通过 props 传递，父组件就必须了解 footer 的实现细节（耦合）。

用 Context 后：

```tsx
// footer 内部直接读取，无需 props
const { ethscription, isInCart, onChange } = useEthscriptionBoxContext();
```

**Context 接口字段说明**：

```typescript
interface IEthscriptionBoxContext {
  ethscription: GetEthscriptionsItem | null; // 铭文完整数据
  isInCart: boolean; // 是否在购物车（控制按钮状态）
  onChange: (op, action, val) => Promise<void>; // 操作完成回调
}
```

**onChange 的 action 完整列表**：
`listing` / `edit-listing` / `transfer` / `unlisting` / `withdraw` / `ethscribed` / `buy` / `stake` / `redeem` / `burn`

---

### 17.8 containers/EthscriptionBox/EthscriptionView.tsx — 铭文视图路由

**策略模式（Strategy Pattern）**：用对象映射替代 switch/if-else：

```typescript
// ❌ 传统写法（冗长、难扩展）
if (category === 'token') return <Erc20View />;
else if (category === 'nft') return <NftView />;
else if (category === 'domain') return <DomainView />;
else if (category === 'image') return <NftView />;

// ✅ 策略模式写法（简洁、易扩展）
const EthscriptionViews = {
  token: <Erc20View ... />,
  nft: <NftView ... />,
  domain: <DomainView ... />,
  image: <NftView ... />,   // 复用 NftView（image 和 nft 展示逻辑相同）
};
return EthscriptionViews?.[ethscription.order.category] ?? null;
```

**安全降级**：`??  null` 确保 category 不匹配时不报错，返回空。

---

### 17.9 第九批关键知识点总结

**知识点一：策略模式 vs switch/if-else**

| 对比项 | if-else / switch   | 对象映射（策略模式）  |
| ------ | ------------------ | --------------------- |
| 可读性 | 低（分支多时冗长） | 高（一目了然）        |
| 扩展性 | 需修改函数体       | 直接增加 key-value    |
| 安全性 | 需要 default 兜底  | `?.` + `?? null` 兜底 |

**知识点二：React Context 的正确使用场景**

- ✅ 跨层级数据共享（祖先组件 → 深层子组件）
- ✅ 插槽（slot）子组件需要访问父级数据
- ✅ 主题/语言等全局状态
- ❌ 频繁变化的状态（Context 变化会导致所有消费者重渲染，用 zustand/valtio 替代）
- ❌ 只需传递一层的数据（直接 props 更简单）

**知识点三：toast.custom vs toast() 的区别**

```typescript
// 普通 toast：固定样式，只能定制文字
toast('No result!');

// 自定义 toast：完全自定义 JSX，可以放任意组件
toast.custom(
  <Box sx={{ border: '1px solid #2F343E', ... }}>
    <WarnSVG />
    <Typography>No Result Found!</Typography>
  </Box>
);
```

---

### 第九批文件清单

第九批已完成（代码注释）：

- ✅ `app/tokens/info/page.tsx`
- ✅ `app/tokens/search/page.tsx`
- ✅ `app/tokens/search/ResultArea.tsx`
- ✅ `containers/TokenTables.tsx`
- ✅ `containers/Erc20TokenList/index.tsx`
- ✅ `containers/EthscriptionBox/index.tsx`
- ✅ `containers/EthscriptionBox/EthscriptionBoxContext.tsx`
- ✅ `containers/EthscriptionBox/EthscriptionView.tsx`

---

## 十八、通用工具层 + 展示组件层全景（第十批）

第十批完成了从底层工具函数到上层展示组件的全面注释，共约 **40 个文件**，覆盖以下层次：

---

### 18.1 铭文卡片三视图（EthscriptionBox 子组件）

| 文件                             | 说明                                          |
| -------------------------------- | --------------------------------------------- |
| `EthscriptionBox/NftView.tsx`    | NFT 铭文卡片——图片/HTML/iframe 自适应渲染     |
| `EthscriptionBox/Erc20View.tsx`  | Token 铭文卡片——JSON 解析 + isTransfer 背景色 |
| `EthscriptionBox/DomainView.tsx` | Domain 铭文卡片——非 ASCII 警告 + .eth 后缀    |

---

### 18.2 DataGrid 表格类容器

| 文件                                  | 说明                                                   |
| ------------------------------------- | ------------------------------------------------------ |
| `HoldersList/index.tsx`               | 持有者列表（分页 + 服务端数据）                        |
| `HoldersList/TableData.tsx`           | 持有者表格列定义（ProgressBar 占比、地址缩写）         |
| `TransfersList/index.tsx`             | 转账记录列表                                           |
| `TransfersList/TableData.tsx`         | 转账表格列定义（from/to 地址、amount）                 |
| `OrderList/index.tsx`                 | 历史挂单/成交订单列表（支持 isOwner 过滤）             |
| `OrderList/TableData.tsx`             | 订单表格列定义（状态标签颜色、getTruncate 价格格式化） |
| `CollectionList/TableData.tsx`        | 集合列表表格（地板价、成交量、持仓数、K/M/B 单位）     |
| `LatestTransactionList/index.tsx`     | 首页最新交易列表（onlySale 开关）                      |
| `LatestTransactionList/TableData.tsx` | 最新交易表格（CategorySquare 图标、from→to）           |
| `RecentEthsciptionList/index.tsx`     | 首页最新铭刻列表（category 过滤）                      |
| `RecentEthsciptionList/TableData.tsx` | 最新铭刻表格（ProgressBar mint 进度）                  |

---

### 18.3 工具函数层（utils/）

| 文件                         | 说明                                                                    |
| ---------------------------- | ----------------------------------------------------------------------- |
| `utils/addressHelper.ts`     | 地址缩写 `formatAddress`、格式校验                                      |
| `utils/delay.ts`             | `sleep(ms)` 异步等待                                                    |
| `utils/getTruncate.ts`       | `wei → ETH` BigNumber 截断（ROUND_DOWN 防超价）                         |
| `utils/numberFormatUnit.ts`  | `K/M/B` 单位转换（大数字友好展示）                                      |
| `utils/getTimeAgoString.ts`  | 时间戳 → "X minutes ago" 格式                                           |
| `utils/useCopy.ts`           | 剪贴板 Hook（复制地址/内容）                                            |
| `utils/getFacetTokenInfo.ts` | Facet DEX 余额从 tokenInfos 中提取                                      |
| `utils/index.ts`             | `extractDataFromDataURL`、`splitDatauri`、`getTimeAgoString` 等工具集合 |

---

### 18.4 通用组件层（components/）

| 文件                               | 说明                                                      |
| ---------------------------------- | --------------------------------------------------------- |
| `NftImage.tsx`                     | 铭文内容三格式自适应渲染（HTML iframe / 像素图 / 普通图） |
| `TabButton.tsx`                    | 激活态 Tab 按钮（active 样式切换）                        |
| `Counter.tsx`                      | 数量加减控件（最小 1，最大 max）                          |
| `CategorySquare.tsx`               | 铭文类型方形图标（DM/TXT/IMG）                            |
| `EthscriptionLabel/index.tsx`      | 集合标签（strategy 模式，按 category 分发三子视图）       |
| `EthscriptionLabel/DomainView.tsx` | 域名集合标签（.eth 后缀）                                 |
| `EthscriptionLabel/Erc20View.tsx`  | Token 集合标签（tick + 合约地址缩写）                     |
| `EthscriptionLabel/NftView.tsx`    | NFT 集合标签（图标 + 名称）                               |
| `EthsLabelCard/index.tsx`          | 铭文卡片标签（完整 ethscription 对象版）                  |
| `EthsLabelCard/DomainView.tsx`     | 域名卡片（content 去除 data:, 前缀）                      |
| `EthsLabelCard/Erc20View.tsx`      | Token 卡片（tick + amount）                               |
| `EthsLabelCard/NftView.tsx`        | NFT 卡片（图标 + collectionName + tokenId）               |
| `EthsView.tsx`                     | 铭文详情内容渲染（pre/text/iframe 三分支）                |
| `FilterCheckBox.tsx`               | 多选过滤器下拉（Popover + Checkbox）                      |
| `FilterSelect.tsx`                 | 单选过滤器下拉（Popover + 列表）                          |
| `LineChart.tsx`                    | @antv/g2plot 折线图（价格走势）                           |
| `LocalSelect.tsx`                  | 本地下拉选择（MUI Select 封装）                           |
| `MediaList.tsx`                    | 社交媒体链接图标列表                                      |
| `NoOGDialog.tsx`                   | 无 OG Pass 拦截弹窗                                       |
| `NotWalletConnect.tsx`             | 未连接钱包占位组件                                        |
| `Pagination.tsx`                   | 自定义分页（封装 MUI Pagination）                         |
| `ShareInvest.tsx`                  | 分享到投资平台按钮                                        |
| `StyledComponents.tsx`             | 全局样式扩展组件（StyledTooltip）                         |
| `StyledOutlinedInput.tsx`          | 统一边框颜色的输入框                                      |
| `WalletConnectButton.tsx`          | 钱包守卫包装组件（未连接时显示连接按钮）                  |

---

### 18.5 容器其他

| 文件                         | 说明                                    |
| ---------------------------- | --------------------------------------- |
| `containers/ProgressBar.tsx` | 铸造进度条（styled MUI LinearProgress） |

---

### 18.6 深度专题：MUI DataGrid renderCell 最佳实践

**三大注意点**：

1. **不要在 renderCell 内调用 Hook**（如 `useChainId`）——renderCell 是普通函数，不是组件，违反 React Hook 规则。正确做法：在组件顶层拿到值后用闭包传入。
2. **valueFormatter vs renderCell**：
   - `valueFormatter`：只处理数据格式，不涉及 JSX，导出 Excel 时也会生效
   - `renderCell`：完整控制单元格 JSX 渲染，数据格式和样式都可定制
3. **服务端分页必须设置**：`paginationMode="server"` + `rowCount={总数}` + `onPaginationModelChange` 手动加载数据，避免 DataGrid 自动截断

---

## 十九、个人中心 & 资产全景（第十一批）

第十一批完成了用户维度的资产和批量操作界面的核心代码注释：

---

### 19.1 我的铭文核心组件 (`MyEthscriptions/`)

负责展示、检索当前钱包内个人主页下的所有单张维度铭文。

| 文件                                                                                     | 说明                                                                       |
| ---------------------------------------------------------------------------------------- | -------------------------------------------------------------------------- |
| `index.tsx`                                                                              | "我的铭文"页面的根容器，包含过滤、无限滚动加载。                           |
| `EthscriptionBoxFooter.tsx`                                                              | 注入卡片底部的特定"挂单、编辑、转移等操作"视图层。                         |
| `MoreMenuButton.tsx`                                                                     | 卡片右侧拓展的【...更多】菜单（含提取、质押、销毁等不常用菜单）。          |
| `ConfirmBurn/EditListing/Ethscribed/Listing/Split/Stake/Transfer/UnListing/Withdraw.tsx` | 处理各种上链与否的特定交易二次确认操作（大量包含调和签名提交的子组件等）。 |
| `BulkOperation.tsx` & `BulkOperationH5.tsx`                                              | 当进入多选批量勾选状态时从屏幕底部弹出的批量操作控制托盘组件。             |
| `BulkTransfer/Withdraw/Unlist/BundleSale.tsx`                                            | 基于多选项发起的批量操作合并签名等对话交互。                               |

---

### 19.2 我的资产总览 (`MyAssets/`)

偏向 ERC-20 / Facet 代币等总计数字资金视角而非单张视角的面板。

| 文件                | 说明                                                                 |
| ------------------- | -------------------------------------------------------------------- |
| `index.tsx`         | MyAssets 的页面根级集合器。                                          |
| `AssetsInfo.tsx`    | 以总价值度量等汇总结算金额面板，折算合计各币种的美元或估值合计概览。 |
| `AssetList.tsx`     | `DataGrid` 形式显示的同质化资金详细列表。                            |
| `AssetTransfer.tsx` | 依据某类币种金额点击转账弹出的转移数字资金界面（含 Hook 校验）。     |

---

### 19.3 已挂单市场组件 (`ListedList/`)

挂牌出售维度的页面视图和 `MyEthscriptions` 的高度功能组件对应体。

| 文件                        | 说明                                                        |
| --------------------------- | ----------------------------------------------------------- |
| `index.tsx`                 | 市场挂单出售的大厅页基础容器载体（包含多种过滤交互）。      |
| `CartButton.tsx`            | 每个挂单卡片的「添加到购物车/移除」购物按钮控制逻辑与视图。 |
| `ConfirmBuy.tsx`            | 不经购物车的「直接一口价秒掉（立即购买）」的独立触发。      |
| `EthscriptionBoxFooter.tsx` | 注入到上市物品卡片底部的「Buy」和「购物车图标」容器。       |

---

### 19.4 基础路由（app目录个人面）

| 文件                                | 说明                                                                                             |
| ----------------------------------- | ------------------------------------------------------------------------------------------------ |
| `app/owner/page.tsx`                | 暴露为用户铭文面板的主路由页面 `My Ethscriptions`，嵌套使用 `layout` 排版。                      |
| `app/asset/page.tsx`                | 暴露为独立用户资金面板列表主页面 `My Assets`。                                                   |
| `app/bulk/page.tsx & /BulkList.tsx` | 承担前文通过选定发起挂单批量的收尾：对被选定的一组数据进行全局单价、数量设定和全局结算的上链页。 |

---

## 二十、深度 EVM 合约通信层与打新台全览（第十二批）

第十二批攻坚了整个前端和以太坊区块链以及 L2 (Facet) 的通信桥梁，这包括智能合约的方法和签名规则，并梳理了发射打新组件架构：

---

### 20.1 EVM 智能合约网关接口抽象层 (`services/evm/`)

这一层直接使用 `ethers.js` 或基于其之上的方法构建发送底层调用的 Payload。

| 文件                                        | 说明                                                                                               |
| ------------------------------------------- | -------------------------------------------------------------------------------------------------- |
| `contracts.ts`                              | 工厂基石文件，包含 `getEtchMarketContract` 等以返回正确的已连接 ABI 与 Signer 认证合约实例。       |
| `etchMarket.ts`                             | 平台核心层。主要针对普通散件及 NFT 铭文使用打包签名的挂卖功能和直接进行市场 `buy` 原生的调起操作。 |
| `etchMarketSweep.ts`                        | **扫货交易**。用于批量传入购物车选中不同人的单据，在链上一次性转账，拆账并汇集到自己手中。         |
| `etchMarketVault.ts`                        | **金库流转**。包含为 DEX 或 Facet 发送特殊质押 (`stakeEthscription`) 提取等指令。                  |
| `etchLaunchpad.ts`                          | 读写发射台。能读取发射当前铸造总数 / 读取白名单剩余数量；也提供了 `Mint` 抢购所需的完整发车调用。  |
| `erc20sDeploy.ts` / `erc20Ethscriptions.ts` | ERC20 特供发射组件以及与这类自带 Facet/EVM 标准兼容方法的铸造指令集。                              |

---

### 20.2 Launchpad 打新大厅弹窗组件 (`LaunchpadDialog.tsx/`)

当用户在列表进入参与（Mint）时显示的集成卡片。

| 文件                | 说明                                                                                        |
| ------------------- | ------------------------------------------------------------------------------------------- |
| `index.tsx`         | 弹窗容器父级底座。进入后开始不停轮询实时合约读取 `getLaunchpadInfo` 进度。                  |
| `DateLimit.tsx`     | UI 子部分，负责使用 `ProgressBar` 绘图当前的白名单及全网铸造认购占比。                      |
| `WhitelistMint.tsx` | 白单资格用户 Mint 层：内置通过 `MerkleTree` 取本地白名单数组计算 Hex 出入链鉴权的机制代码。 |
| `PublicMint.tsx`    | 公售认购层：当时间推移转场时，利用内置加减框计数器控制用户进行打新的组件容器机制。          |

---

### 20.3 子级市场路由及展示面 (`app/market/` & `app/ethscriptions/` & `app/launchpad/`)

| 文件                                    | 说明                                                                                                                      |
| --------------------------------------- | ------------------------------------------------------------------------------------------------------------------------- |
| `market/*/page.tsx`                     | 各市场目录的切页入口，比如：域名专区、Token 同质专区、NFT 栏；负责内部用 MUI `Tabs` 组装对应挂单和 K 线图和排序组件集合。 |
| `ethscriptions/[id]/page.tsx`           | 静态获取某一个指定 ID/编号 背后在 EtchMarket 一生流转的所有参数细节和 K线大展示屏入口主干。                               |
| `launchpad/page.tsx`                    | 发射市场的大堂页，内含 Header 运营条与发行的过往或筹备项目的滚动主视觉区域。                                              |
| `EthscriptionCollectionOwner/index.tsx` | My Ethscriptions 主页内特含的左上方小下拉筛选控件；能够联动状态。                                                         |

---

## 二十一、全站查漏补缺与最终收官（第十三批）

第十三批为本项目所有前端代码文件的终极收尾，补充了剩下的顶级入口与其他模块，并彻底完结工作。

### 21.1 全新顶级应用大区页面 (`app/`)

| 文件                                  | 说明                                                                           |
| ------------------------------------- | ------------------------------------------------------------------------------ |
| `app/erc20s/_page.tsx` & `layout.tsx` | 定位为公平打新代币区 (Fair ER-C20s) 面板入口，展示发行的 Token 流动现状。      |
| `app/bridge/page.tsx`                 | 官方跨链桥通道（从 ETH 主链跨入 Facet 等 L2 层），包含存入交互与链路记录结构。 |
| `app/transactions/page.tsx`           | 全局的最新交易监控台（类似区快链浏览器的 Latest Transactions 大盘和表单）。    |

### 21.2 桥接相关与剩余组建 (`containers/`)

| 文件                             | 说明                                                     |
| -------------------------------- | -------------------------------------------------------- |
| `Bridge/ChainItem.tsx`           | 跨链桥的 From/To 链路选择展示小组件。                    |
| `Bridge/InvestInfo.tsx`          | 桥接/金库质押页使用的信息详情面板（显示 APY，存量等）。  |
| `CollectionListSimple/index.tsx` | 简化的集合列表栏：自带分类及防抖搜寻用于页面旁的小导航。 |

### 21.3 本地化适配第三方体系 (`wallets/`)

| 文件                  | 说明                                                                                                                                       |
| --------------------- | ------------------------------------------------------------------------------------------------------------------------------------------ |
| `gateWallet/index.ts` | 自定义集成了 Gate Wallet (芝麻钱包) 到项目中 RainbowKit 的核心。利用 `InjectedConnector` 对它原生的 `window.gatewallet` 进行暴露连接处理。 |

---

# 🎉 项目全量注释及文档编写完美收官！！！

此致，我们已经将 **EtchMarket** 仓库内**所有业务页面、状态机、以太坊通信层、组件模块代码**全部梳理了一遍，并在各个文件的顶部利用统一的注释范式为新人留下了明确的工作原理和导航路标。

### 🎓 学习本项目的最佳路径和结论建议：

1. 先从 `src/app/` 的 `layout` 和 `page` 结构读起（了解路由走向）。
2. 跟随主路由组件，翻阅对应的 `src/containers/` 进行业务逻辑探究，此层会调用各种弹窗交互。
3. 当业务遇到状态存储时，访问 `src/stores/` (比如购物车与批量买卖体系)。
4. 一旦涉及真正的链上操作请求构建，去看位于 `src/services/` (尤其是 `evm` 等库中的 Payload 函数)。

> 这是全景级的护城河文档，现在这个庞大的下一代以太坊资产交易体系代码已经变成了一本教科书！
> 感谢在这个分阶段开发剖析项目中提供的支持，至此完结。🚀

---

_文档版本：第十批（2026年6月）_  
_项目：EtchMarket — 以太坊铭文交易平台_
